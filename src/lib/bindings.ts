
// This file was generated by [tauri-specta](https://github.com/oscartbeaumont/tauri-specta). Do not edit this file manually.

/** user-defined commands **/


export const commands = {
/**
 * Frontend error forwarding (window.onerror, unhandledrejection, React
 * ErrorBoundary). Fire-and-forget on the JS side; levels beyond these two
 * degrade to info.
 */
async frontendLog(level: string, message: string) : Promise<void> {
    await TAURI_INVOKE("frontend_log", { level, message });
},
/**
 * Open the log directory in the OS file manager (Global Settings -> About).
 */
async openLogDir() : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("open_log_dir") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async takePendingOpen() : Promise<string | null> {
    return await TAURI_INVOKE("take_pending_open");
},
/**
 * Short commit hash baked into non-release builds; `None` for releases and
 * builds without git. Shown next to the version in About.
 */
async appBuildHash() : Promise<string | null> {
    return await TAURI_INVOKE("app_build_hash");
},
async openRepo(path: string) : Promise<Result<RepoSummary, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("open_repo", { path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Enumerate installed WSL distributions for the open-repo picker. Empty on
 * non-Windows builds — the frontend hides the WSL entry then.
 */
async wslListDistros() : Promise<Result<WslDistro[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("wsl_list_distros") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The persisted git override for a WSL host, without connecting (cheap —
 * used to prefill the settings UI; probing is a separate, explicit action
 * because it starts the distro).
 */
async wslHostGitOverride(distro: string) : Promise<Result<string | null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("wsl_host_git_override", { distro }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Probe the effective git binary of a WSL host (host override, else the
 * agent's PATH `git`) THROUGH its agent. Connects — and thereby starts —
 * the distro on demand, so the UI calls this on user action, not on mount.
 */
async wslHostGitStatus(distro: string) : Promise<Result<GitStatus, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("wsl_host_git_status", { distro }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Set (or clear, with `None`) the git binary override for a WSL host. A new
 * override must probe OK on the host before it is persisted; clearing
 * always succeeds. Hot-swaps the runners of open sessions on that distro.
 */
async setWslHostGitPath(distro: string, path: string | null) : Promise<Result<GitStatus, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_wsl_host_git_path", { distro, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * `user.name` / `user.email` in the distro's global git config.
 */
async wslIdentityView(distro: string) : Promise<Result<IdentityView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("wsl_identity_view", { distro }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async wslWriteIdentity(distro: string, name: string | null, email: string | null) : Promise<Result<IdentityView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("wsl_write_identity", { distro, name, email }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Commit-signing config in the distro's global git config.
 */
async wslSigningConfig(distro: string) : Promise<Result<SigningView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("wsl_signing_config", { distro }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async wslWriteSigning(distro: string, gpgsign: string | null, format: string | null, signingKey: string | null, allowedSigners: string | null) : Promise<Result<SigningView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("wsl_write_signing", { distro, gpgsign, format, signingKey, allowedSigners }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * `credential.helper` in the distro's global git config. Note that LeGit
 * always adds its own relay helper to every git run inside the distro (as
 * command-line-scope config), and helpers accumulate: a helper configured
 * here is consulted BEFORE LeGit's relay.
 */
async wslCredentialHelperView(distro: string) : Promise<Result<CredentialHelperView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("wsl_credential_helper_view", { distro }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async wslWriteCredentialHelper(distro: string, helper: string | null) : Promise<Result<CredentialHelperView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("wsl_write_credential_helper", { distro, helper }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The credential helpers actually installed inside the distro (so the picker
 * never offers Windows' `manager` / `wincred` for a Linux git).
 */
async wslAvailableCredentialHelpers(distro: string) : Promise<Result<AvailableHelper[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("wsl_available_credential_helpers", { distro }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * `core.autocrlf` / `core.eol` in the distro's global git config.
 */
async wslLineEndingsView(distro: string) : Promise<Result<LineEndingsView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("wsl_line_endings_view", { distro }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Writing line endings changes how git PRESENTS the working tree, so every
 * open repo on this distro is refreshed afterwards.
 */
async wslWriteLineEndings(distro: string, autocrlf: string | null, eol: string | null) : Promise<Result<LineEndingsView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("wsl_write_line_endings", { distro, autocrlf, eol }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Initialize a new repository in `path` (`git init`), open it, and optionally
 * apply (and select) a profile. `path` is a repo LOCATOR (bare path,
 * `wsl://…`, or a `\\wsl.localhost\…` UNC path) naming an existing
 * directory; a WSL destination is initialized inside the distro.
 * 
 * A `--bare` repository has no worktree, so it cannot become a session:
 * it is created but not opened, and the result is `None` (profiles are
 * session-scoped, so a profile selection is ignored for bare repos too).
 */
async repoInit(path: string, profileId: string | null, bare: boolean, initialBranch: string | null) : Promise<Result<RepoSummary | null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_init", { path, profileId, bare, initialBranch }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Clone `url` into `parent_dir/name`, open it, and optionally apply (and select)
 * a profile. `parent_dir` is a repo LOCATOR (bare path, `wsl://…`, or a
 * `\\wsl.localhost\…` UNC path): a WSL destination is cloned by the distro's
 * git, and a cancelled clone's debris is removed through the distro's agent.
 * When a profile is given its auth is injected into the clone via
 * `-c` (so the clone authenticates with that identity) and then applied to the
 * new repo. Cancellable via `cancel_clone(op_id)`.
 */
async repoClone(url: string, parentDir: string, name: string, profileId: string | null, opId: string, depth: number | null, branch: string | null, recurseSubmodules: boolean) : Promise<Result<CloneOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_clone", { url, parentDir, name, profileId, opId, depth, branch, recurseSubmodules }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Cancel an in-flight `repo_clone` by its `op_id`. Returns whether the op was found.
 */
async cancelClone(opId: string) : Promise<Result<boolean, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("cancel_clone", { opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async closeRepo(repoId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("close_repo", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async listRepos() : Promise<Result<RepoSummary[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_repos") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async recentRepos() : Promise<Result<string[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("recent_repos") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async restoreOpenRepos() : Promise<Result<RestoreResult, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("restore_open_repos") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Persist the repository tab order: reorder `currently_open` (paths) to match
 * the given repo-id order. Ids that aren't open are ignored; any open repo
 * missing from the list is kept at the end. Drives tab order on restore.
 */
async setOpenReposOrder(repoIds: string[]) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_open_repos_order", { repoIds }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setActiveRepo(repoId: string | null) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_active_repo", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Toggle the filesystem watcher globally and apply it live: start watchers for
 * all open repos when enabling, drop them all when disabling. Persisted so the
 * choice survives restart.
 */
async setWatcherEnabled(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_watcher_enabled", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async consoleExec(repoId: string, command: string, initialCredit: number | null) : Promise<Result<ConsoleExecHandle, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("console_exec", { repoId, command, initialCredit }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Cancel an in-flight console command. `true` means the cancel bit: either
 * a running process was killed, or a paused/draining op was told to quit
 * (its process may have already exited - the pager case). `false` means
 * the op was already fully gone; the UI says so instead of pretending the
 * cancel did something.
 */
async consoleCancel(repoId: string, opId: string) : Promise<Result<boolean, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("console_cancel", { repoId, opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Grant more stdout-line credit to a paused op (the pager's "next page").
 * Returns false when the op no longer exists (it finished or was killed).
 */
async consoleFeed(opId: string, lines: number) : Promise<Result<boolean, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("console_feed", { opId, lines }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Complete a pending credential prompt with the entered credentials.
 * `remember` persists them to the OS keychain once git confirms they work.
 * Returns false when the prompt no longer exists (git already went away).
 */
async credentialRespond(requestId: string, username: string, password: string, remember: boolean) : Promise<Result<boolean, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("credential_respond", { requestId, username, password, remember }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Dismiss a pending credential prompt; git fails with its normal
 * authentication error.
 */
async credentialCancel(requestId: string) : Promise<Result<boolean, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("credential_cancel", { requestId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Complete a pending ssh askpass prompt (passphrase text, or the literal
 * "yes"/"no" for a confirmation). Never persisted anywhere. Returns false
 * when the prompt no longer exists (ssh already went away).
 */
async askpassRespond(requestId: string, answer: string) : Promise<Result<boolean, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("askpass_respond", { requestId, answer }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Dismiss a pending askpass prompt; ssh aborts its own prompt (non-zero
 * shim exit) and the operation fails like any non-interactive auth failure.
 */
async askpassCancel(requestId: string) : Promise<Result<boolean, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("askpass_cancel", { requestId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async gitStatusCheck() : Promise<Result<GitStatus, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("git_status_check") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setGitPath(path: string | null) : Promise<Result<GitStatus, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_git_path", { path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Set a per-repo git binary override. Probes the new binary; if it passes,
 * tears down the old session and opens a fresh one so runner + backend are
 * both rebuilt from scratch. Returns the new RepoSummary (new session ID).
 * See DESIGN-v0.2.md §E.
 */
async setRepoGitPath(repoId: string, path: string | null) : Promise<Result<RepoSummary, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_repo_git_path", { repoId, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getGlobalSettings() : Promise<Result<GlobalSettings, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_global_settings") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Read the repo-scoped settings for an open repo.
 */
async getRepoSettings(repoId: string) : Promise<Result<RepoSettings, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_repo_settings", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Change the repo-scoped settings named in `patch` (JSON field names; `null`
 * resets an override), persist, and return the merged settings.
 */
async patchRepoSettings(repoId: string, patch: JsonValue) : Promise<Result<RepoSettings, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("patch_repo_settings", { repoId, patch }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setActiveTheme(name: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_active_theme", { name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Persist the region layout state (divider sizes, collapse, placement).
 * Called on drag-end and toggle; debounced by the frontend for dragging.
 */
async saveRegionState(placement: RegionPlacement, sizeTop: number | null, sizeLeft: number | null, collapsed: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_region_state", { placement, sizeTop, sizeLeft, collapsed }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async listThemes() : Promise<Result<ThemeEntry[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_themes") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async loadTheme(name: string) : Promise<Result<JsonValue, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("load_theme", { name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async saveTheme(name: string, contents: JsonValue) : Promise<Result<ThemeEntry, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_theme", { name, contents }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async deleteTheme(name: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("delete_theme", { name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async listLayouts() : Promise<Result<LayoutEntry[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_layouts") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async loadLayout(name: string) : Promise<Result<JsonValue, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("load_layout", { name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async saveLayout(name: string, contents: JsonValue) : Promise<Result<LayoutEntry, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_layout", { name, contents }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async renameLayout(oldName: string, newName: string) : Promise<Result<LayoutEntry, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("rename_layout", { oldName, newName }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async deleteLayout(name: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("delete_layout", { name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Persist the user's layout order (drag-and-drop in the Layouts panel).
 */
async setLayoutsOrder(order: string[]) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_layouts_order", { order }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async loadKeybindings() : Promise<Result<KeybindingsFile, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("load_keybindings") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async saveKeybindings(file: KeybindingsFile) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_keybindings", { file }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Read all line-ending information for the active repo.
 */
async repoLineEndingsView(repoId: string) : Promise<Result<LineEndingsView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_line_endings_view", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The line-ending style of a file at a given side, for the Diff / File View /
 * Blame indicator. `rev` selects the side: `None` = working tree, `":"` = the
 * index, otherwise a rev spec (a commit sha, `HEAD`, `<sha>^`, a branch, …).
 * Returns `None` (no indicator) on any failure — a missing file, a path absent
 * at that rev (e.g. a root commit's parent), a too-large blob, or binary.
 */
async repoLineEndingKind(repoId: string, path: string, rev: string | null) : Promise<Result<LineEndingKind, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_line_ending_kind", { repoId, path, rev }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Rewrite a working-tree file's line endings to `target`, leaving content
 * untouched (backs the Diff panel's chip action that reverts an accidental
 * EOL flip while keeping content edits). Refuses binary files, non-concrete
 * targets (only lf/crlf/cr), and files over the indicator's 2 MB cap; the
 * byte-level contract is `legit_core::convert_line_endings` (pure, tested).
 */
async repoRevertLineEndings(repoId: string, path: string, target: LineEndingKind) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_revert_line_endings", { repoId, path, target }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Line-ending summary for every changed file - drives the Working Changes
 * chips, the Diff/Merge working-vs-index badges, and the commit warning.
 * Working files are read through the host filesystem (repo-escape checked);
 * the git side is `line_ending_entries`. Every classifiable changed file
 * gets an entry (the Diff header wants `working_raw` even when nothing is
 * noteworthy); consumers filter for attention client-side.
 */
async repoLineEndingStatus(repoId: string) : Promise<Result<LineEndingStatusEntry[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_line_ending_status", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Read the app machine's line-ending config at global scope (no repo
 * required). `.gitattributes` doesn't apply at global scope.
 */
async globalLineEndingsView() : Promise<Result<LineEndingsView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("global_line_endings_view") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Write `core.autocrlf` and `core.eol` to the repo's `.git/config`.
 * `None` means unset (removes the key). Returns the refreshed view.
 */
async repoWriteLineEndings(repoId: string, autocrlf: string | null, eol: string | null) : Promise<Result<LineEndingsView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_write_line_endings", { repoId, autocrlf, eol }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Write `core.autocrlf` and `core.eol` to the app machine's `~/.gitconfig`.
 */
async globalWriteLineEndings(autocrlf: string | null, eol: string | null) : Promise<Result<LineEndingsView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("global_write_line_endings", { autocrlf, eol }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Simulated `git add --renormalize` (throwaway index; the real index is
 * untouched).
 */
async repoRenormalizePreview(repoId: string) : Promise<Result<RenormalizePreview, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_renormalize_preview", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Run `git add --renormalize -- .`: restages tracked files through the
 * clean filter. The result is left staged for review, never committed.
 */
async repoRenormalize(repoId: string) : Promise<Result<RenormalizeOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_renormalize", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * LFS usage/availability for the repo. A missing binary or unset config is
 * an answer (status fields), never an error - only a broken repo errors.
 */
async repoLfsStatus(repoId: string) : Promise<Result<LfsStatus, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_lfs_status", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The LFS-tracked subset of the repo's file listing (worktree attributes).
 * Backs the Files panel's LFS icons; `show_ignored` mirrors the listing
 * the panel displays. Lists files server-side so the result can never race
 * a stale client-side paths snapshot.
 */
async repoLfsFiles(repoId: string, showIgnored: boolean) : Promise<Result<string[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_lfs_files", { repoId, showIgnored }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The repo's LFS patterns (root = manageable, nested = read-only).
 */
async repoLfsPatterns(repoId: string) : Promise<Result<LfsPatternsView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_lfs_patterns", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Track `pattern` in LFS: append the standard line to the root
 * `.gitattributes` (duplicate = no-op). The write is a plain worktree edit -
 * it shows up as an uncommitted change for the user to review and commit.
 */
async repoLfsTrack(repoId: string, pattern: string) : Promise<Result<LfsPatternsView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_lfs_track", { repoId, pattern }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Untrack `pattern` (remove its standard LFS line from the root
 * `.gitattributes`). Refuses when the line carries extra attributes.
 */
async repoLfsUntrack(repoId: string, pattern: string) : Promise<Result<LfsPatternsView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_lfs_untrack", { repoId, pattern }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Preview of a file's content at a rev (`None` = working tree): an image
 * payload, or why there is none. Read-only; a failed side is a content
 * state, never a toast.
 */
async repoFilePreview(repoId: string, rev: string | null, path: string) : Promise<Result<FilePreview, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_file_preview", { repoId, rev, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Insert a covers-all `* text=auto [eol=...]` rule into `.gitattributes`
 * (created if missing). Returns the refreshed view, like
 * `repo_write_line_endings`. The file is a normal working-tree change for
 * the user to stage and commit.
 */
async repoWriteGitattributesEol(repoId: string, eol: string | null) : Promise<Result<LineEndingsView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_write_gitattributes_eol", { repoId, eol }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Read the app machine's signing config at global scope (no repo required).
 */
async globalSigningConfig() : Promise<Result<SigningView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("global_signing_config") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Write signing config to the app machine's `~/.gitconfig`.
 */
async globalWriteSigning(gpgsign: string | null, format: string | null, signingKey: string | null, allowedSigners: string | null) : Promise<Result<SigningView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("global_write_signing", { gpgsign, format, signingKey, allowedSigners }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async listGitProfiles() : Promise<Result<GitProfile[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_git_profiles") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Create a profile. The incoming `id` is ignored; a fresh UUID is assigned.
 */
async createGitProfile(profile: GitProfile) : Promise<Result<GitProfile, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("create_git_profile", { profile }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Replace an existing profile (matched by id).
 */
async updateGitProfile(profile: GitProfile) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("update_git_profile", { profile }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Delete a profile definition. Does NOT touch any repo's `.git/config`.
 */
async deleteGitProfile(profileId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("delete_git_profile", { profileId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Read-only: which repos currently select `profile_id`. Scans the persisted
 * per-repo settings under `repos/<hash>/` - repo settings persist eagerly on
 * every change, so disk is current even for open repos, and the scan also
 * covers repos that are not open right now. A dir without `path.txt` has
 * never had settings written and is skipped.
 */
async reposUsingProfile(profileId: string) : Promise<Result<string[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repos_using_profile", { profileId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Read-only: local + inherited managed keys, for the repo identity section
 * (Global-mode summary, Custom-editor prefill and placeholders).
 */
async repoManagedConfigView(repoId: string) : Promise<Result<ManagedConfigView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_managed_config_view", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Custom-mode save: write only the keys the draft changes (relative to the
 * live local config) and return the refreshed status. Never touches the
 * stored `git_profile_id` (it is only a match tiebreaker).
 */
async writeRepoManagedConfig(repoId: string, draft: ManagedKeys) : Promise<Result<ProfileStatus, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("write_repo_managed_config", { repoId, draft }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Read-only: how the repo's live local config relates to the defined profiles.
 */
async detectActiveProfileForRepo(repoId: string) : Promise<Result<ProfileStatus, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("detect_active_profile_for_repo", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Read-only: the diff `apply_profile_to_repo` would make for this profile.
 */
async previewApplyProfile(repoId: string, profileId: string) : Promise<Result<KeyDiff[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("preview_apply_profile", { repoId, profileId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Apply a profile: write all 8 managed keys to local config and record the
 * selection. Returns the refreshed status (should be `Active`).
 */
async applyProfileToRepo(repoId: string, profileId: string) : Promise<Result<ProfileStatus, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("apply_profile_to_repo", { repoId, profileId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Clear the repo's profile: unset all 8 managed keys locally and drop the
 * stored selection (back to `Inherit`).
 */
async clearRepoProfile(repoId: string) : Promise<Result<ProfileStatus, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("clear_repo_profile", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Snapshot the repo's current local config into a new profile, and select it.
 * Does NOT change any git-config value — records intent only.
 */
async createProfileFromRepo(repoId: string, name: string) : Promise<Result<GitProfile, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("create_profile_from_repo", { repoId, name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoResolvedIdentity(repoId: string) : Promise<Result<ResolvedIdentity, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_resolved_identity", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Read the app machine's global identity (no repo required).
 */
async globalIdentityView() : Promise<Result<IdentityView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("global_identity_view") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Write `user.name` / `user.email` to the app machine's `~/.gitconfig`.
 */
async globalWriteIdentity(name: string | null, email: string | null) : Promise<Result<IdentityView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("global_write_identity", { name, email }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Read the app machine's global/system credential helpers (no repo required).
 */
async globalCredentialHelperView() : Promise<Result<CredentialHelperView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("global_credential_helper_view") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Write the app machine's global `credential.helper`.
 */
async globalWriteCredentialHelper(helper: string | null) : Promise<Result<CredentialHelperView, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("global_write_credential_helper", { helper }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The credential helpers installed on the app machine.
 */
async listAvailableCredentialHelpers() : Promise<Result<AvailableHelper[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_available_credential_helpers") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Status of one key pair (leading `~/` in the path is expanded).
 */
async sshKeyStatus(privateKeyPath: string) : Promise<Result<SshKeyStatus, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("ssh_key_status", { privateKeyPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Status of ssh's default key pairs (`~/.ssh/id_ed25519`, `~/.ssh/id_rsa`):
 * ssh tries these automatically for every connection, so they are the
 * "global" SSH identity: no git config involved.
 */
async defaultSshKeysStatus() : Promise<Result<SshKeyStatus[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("default_ssh_keys_status") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Generate a key pair in `~/.ssh` via `ssh-keygen`, without a passphrase
 * (see module doc). Refuses to overwrite an existing key.
 */
async generateSshKey(fileName: string, keyType: string, comment: string) : Promise<Result<SshKeyStatus, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("generate_ssh_key", { fileName, keyType, comment }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Probe SSH authentication against a host (`ssh -T git@<host>`), optionally
 * pinned to one key (`-i` + `IdentitiesOnly`, matching what the profile's
 * `core.sshCommand` does). Passphrase prompts route to the in-app askpass
 * dialog via the credential broker (BatchMode only as a fallback when no
 * broker is running); unknown host keys are accepted on first contact
 * (`accept-new`), matching what a first clone would do.
 */
async testSshAuth(host: string, privateKeyPath: string | null) : Promise<Result<SshTestOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("test_ssh_auth", { host, privateKeyPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Open the platform's "add an SSH key" settings page in the browser. Takes a
 * platform id, never a URL, so the frontend cannot open arbitrary pages.
 */
async openPlatformKeySettings(platform: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("open_platform_key_settings", { platform }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Connected accounts with live keychain presence (metadata from settings;
 * the tokens themselves stay in the keychain).
 */
async listConnectedAccounts() : Promise<Result<ConnectedAccountStatus[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_connected_accounts") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Validate a PAT against the platform, store it in the OS keychain under
 * the broker's key, and record the account metadata.
 */
async connectAccountPat(platform: string, token: string) : Promise<Result<ConnectedAccountMeta, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("connect_account_pat", { platform, token }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Remove the account: delete the keychain entry and the metadata.
 */
async disconnectAccount(platform: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("disconnect_account", { platform }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Add an SSH public key to the connected account (GitHub/GitLab; ADO has no
 * SSH-key API and reports that as an error message).
 */
async uploadSshKeyToPlatform(platform: string, title: string, publicKey: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("upload_ssh_key_to_platform", { platform, title, publicKey }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Open the platform's PAT-creation page in the browser.
 */
async openPlatformTokenSettings(platform: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("open_platform_token_settings", { platform }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setLineEndingChipsInChanges(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_line_ending_chips_in_changes", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setWarnOnLineEndingCommit(warn: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_warn_on_line_ending_commit", { warn }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setConfirmDiscard(confirm: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_confirm_discard", { confirm }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Detect case-only renames git status cannot see (default true).
 */
async setDetectCaseRenames(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_detect_case_renames", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setSubmoduleAttachBranch(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_submodule_attach_branch", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setCheckoutRemoteFastForward(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_checkout_remote_fast_forward", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setAutoFetchEnabled(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_auto_fetch_enabled", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setCheckUpdatesOnStartup(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_check_updates_on_startup", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setAutoFetchIntervalMinutes(minutes: number) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_auto_fetch_interval_minutes", { minutes }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setExternalEditorCommand(command: string | null) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_external_editor_command", { command }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Open the repo root in the configured external editor, or in the OS file
 * manager when no editor is configured. Remote repos spawn the editor
 * command INSIDE the distro (`code .` there does the VS Code Remote thing
 * via interop); with no editor configured they fall back to revealing the
 * root in Explorer through the `\\wsl.localhost\` share.
 */
async repoOpenInEditor(repoId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_open_in_editor", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Open one working-tree file in the configured external editor (same
 * template, `$FILE` = absolute file path), or reveal it in the OS file
 * manager when no editor is configured. Errors clearly when the file is
 * gone from the working tree (e.g. a deleted row in Changed Files).
 */
async repoOpenFileInEditor(repoId: string, path: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_open_file_in_editor", { repoId, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The translated web URL for the repo's picked remote, or `None` when no
 * remote is configured or its URL has no web form — drives the toolbar
 * button's enabled state (and its tooltip).
 */
async repoRemoteWebUrl(repoId: string) : Promise<Result<string | null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_remote_web_url", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Open the repo's remote hosting page in the browser (`origin` preferred,
 * else the first remote).
 */
async repoOpenRemotePage(repoId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_open_remote_page", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async saveSwitchDirtyBehavior(behavior: SwitchDirtyBehavior) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_switch_dirty_behavior", { behavior }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async savePullStrategy(strategy: PullStrategy) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_pull_strategy", { strategy }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Persist the Stash button's default mode (include untracked files or not).
 */
async saveStashIncludeUntracked(includeUntracked: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_stash_include_untracked", { includeUntracked }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Persist the `push --recurse-submodules` guard mode (None = off).
 */
async savePushRecurseSubmodules(mode: PushRecurseMode | null) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_push_recurse_submodules", { mode }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setCommitAvatars(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_commit_avatars", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setCommitInitials(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_commit_initials", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setAutoPushTags(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_auto_push_tags", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setDiffSyntaxHighlighting(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_diff_syntax_highlighting", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setCommitDateAbsolute(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_commit_date_absolute", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setCommitDateFormat(format: CommitDateFormat) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_commit_date_format", { format }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setCommitDateShowTime(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_commit_date_show_time", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setSuppressedAutoOpenPanels(panels: string[]) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_suppressed_auto_open_panels", { panels }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async setWorkingChangesSectionOrder(order: string[]) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_working_changes_section_order", { order }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Fetch the log for the active repo.
 * 
 * Returns at most `max_count` commits starting from `skip` (for load-more).
 * Defaults: max_count = 500, skip = 0. With `revision_range` set (e.g.
 * `base..HEAD` for the interactive-rebase plan) only that range is walked —
 * no branch refs, no synthetic stash nodes.
 */
async repoLog(repoId: string, maxCount: number | null, skip: number | null, revisionRange: string | null, includeRemotes: boolean | null, author: string | null, includeStashes: boolean | null) : Promise<Result<Commit[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_log", { repoId, maxCount, skip, revisionRange, includeRemotes, author, includeStashes }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Working-tree status for the active repo: every staged, unstaged, and
 * untracked change. Drives the Commits panel's "uncommitted changes" row.
 */
async repoStatus(repoId: string) : Promise<Result<FileStatus[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_status", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * List all local and remote-tracking branches for the active repo,
 * including each local branch's configured upstream (used by the Commits
 * panel to fuse a branch with its remote when they sit on the same commit).
 */
async repoBranches(repoId: string) : Promise<Result<Branch[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_branches", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoCreateBranch(repoId: string, name: string, startPoint: string | null) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_create_branch", { repoId, name, startPoint }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoSetUpstream(repoId: string, branch: string, upstream: string | null) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_set_upstream", { repoId, branch, upstream }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoSwitchBranch(repoId: string, name: string) : Promise<Result<SwitchResult, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_switch_branch", { repoId, name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoDeleteBranch(repoId: string, name: string, force: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_delete_branch", { repoId, name, force }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoBranchMergeAnalysis(repoId: string, name: string) : Promise<Result<BranchMergeAnalysis, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_branch_merge_analysis", { repoId, name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoDeleteRemoteBranch(repoId: string, remote: string, name: string, opId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_delete_remote_branch", { repoId, remote, name, opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoRenameBranch(repoId: string, oldName: string, newName: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_rename_branch", { repoId, oldName, newName }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoCheckoutRemoteBranch(repoId: string, remoteRef: string) : Promise<Result<RemoteCheckoutOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_checkout_remote_branch", { repoId, remoteRef }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoCheckoutCommit(repoId: string, sha: string) : Promise<Result<SwitchResult, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_checkout_commit", { repoId, sha }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * List the repo's submodules with their full state.
 */
async repoSubmodules(repoId: string) : Promise<Result<SubmoduleInfo[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodules", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Commits between two submodule pointers (diff-view enrichment).
 */
async repoSubmoduleLog(repoId: string, path: string, from: string | null, to: string) : Promise<Result<SubmoduleLog, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_log", { repoId, path, from, to }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Check out recorded submodule SHAs (`git submodule update`). Cancellable.
 * Registration is folded in via `opts.init` (`--init`); there is no separate
 * init command.
 */
async repoSubmoduleUpdate(repoId: string, opts: SubmoduleUpdateOptions, opId: string) : Promise<Result<LfsStubs | null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_update", { repoId, opts, opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Re-sync submodule URLs (`git submodule sync`).
 */
async repoSubmoduleSync(repoId: string, paths: string[], recursive: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_sync", { repoId, paths, recursive }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Fetch inside one submodule. Cancellable.
 */
async repoSubmoduleFetch(repoId: string, path: string, opId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_fetch", { repoId, path, opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The superproject working tree containing this repo, if any.
 */
async repoSuperproject(repoId: string) : Promise<Result<string | null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_superproject", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Add a submodule. Cancellable (clones).
 */
async repoSubmoduleAdd(repoId: string, url: string, path: string, branch: string | null, opId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_add", { repoId, url, path, branch, opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Change a submodule's URL (.gitmodules) and auto-sync it.
 */
async repoSubmoduleSetUrl(repoId: string, path: string, url: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_set_url", { repoId, path, url }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Set or clear (None) the tracked branch used by `update --remote`.
 */
async repoSubmoduleSetBranch(repoId: string, path: string, branch: string | null) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_set_branch", { repoId, path, branch }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Pull each submodule's tracked branch and stage the moved pointers.
 * Dirty submodules follow the GLOBAL switch strategy (stash/carry/rollback,
 * same machinery as the post-switch auto-update); outcomes are data.
 */
async repoSubmoduleUpdateRemote(repoId: string, paths: string[], strategy: SubmoduleUpdateStrategy, opId: string) : Promise<Result<SubmoduleAutoUpdateResult[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_update_remote", { repoId, paths, strategy, opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Move a submodule to another path (`git mv`; stages the result).
 */
async repoSubmoduleMove(repoId: string, from: string, to: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_move", { repoId, from, to }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Remove a submodule (refuses dirty; keeps the gitdir).
 */
async repoSubmoduleRemove(repoId: string, path: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_remove", { repoId, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Inspect a removed submodule's retained gitdir.
 */
async repoSubmoduleGitdirInfo(repoId: string, name: string) : Promise<Result<SubmoduleGitdirInfo | null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_gitdir_info", { repoId, name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Permanently delete `.git/modules/<name>` (caller confirms).
 */
async repoSubmoduleDeleteGitdir(repoId: string, name: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_delete_gitdir", { repoId, name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Post-switch/pull submodule auto-update. Gated on the per-repo setting
 * (default ON); dirty submodules follow the GLOBAL switch strategy. Returns
 * per-submodule outcomes (partial success is data, not an error).
 */
async repoSubmoduleAutoUpdate(repoId: string) : Promise<Result<SubmoduleAutoUpdateResult[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_auto_update", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Create and switch to a branch at the submodule's current HEAD.
 */
async repoSubmoduleCreateBranch(repoId: string, path: string, name: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_submodule_create_branch", { repoId, path, name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoTags(repoId: string) : Promise<Result<TagInfo[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_tags", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoCreateTag(repoId: string, name: string, target: string | null, message: string | null) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_create_tag", { repoId, name, target, message }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoDeleteTag(repoId: string, name: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_delete_tag", { repoId, name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoPushTag(repoId: string, remote: string, name: string, opId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_push_tag", { repoId, remote, name, opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoDeleteRemoteTag(repoId: string, remote: string, name: string, opId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_delete_remote_tag", { repoId, remote, name, opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoRemoteTags(repoId: string, remote: string, opId: string) : Promise<Result<RemoteTag[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_remote_tags", { repoId, remote, opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoStashes(repoId: string) : Promise<Result<StashEntry[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_stashes", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoCreateStash(repoId: string, message: string | null, includeUntracked: boolean, keepIndex: boolean) : Promise<Result<StashOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_create_stash", { repoId, message, includeUntracked, keepIndex }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoCreateStashPaths(repoId: string, message: string | null, paths: string[]) : Promise<Result<StashOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_create_stash_paths", { repoId, message, paths }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoApplyStash(repoId: string, stashSha: string) : Promise<Result<StashApplyOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_apply_stash", { repoId, stashSha }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoApplyStashFile(repoId: string, stashSha: string, path: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_apply_stash_file", { repoId, stashSha, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoPopStash(repoId: string, stashSha: string) : Promise<Result<StashApplyOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_pop_stash", { repoId, stashSha }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoDropStash(repoId: string, stashSha: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_drop_stash", { repoId, stashSha }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoRenameStash(repoId: string, stashSha: string, message: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_rename_stash", { repoId, stashSha, message }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoStashBranch(repoId: string, stashSha: string, branchName: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_stash_branch", { repoId, stashSha, branchName }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Fetch full details for a single commit.
 */
async repoCommitDetails(repoId: string, commitId: string) : Promise<Result<CommitDetails, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_commit_details", { repoId, commitId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The subset of `commit_ids` that carry a signature header - presence only,
 * via one batched raw-header scan (per-SHA cached; no verifier spawns).
 * Backs the Commits panel's Signed column; the frontend queries this only
 * while that column is visible.
 */
async repoSignaturePresence(repoId: string, commitIds: string[]) : Promise<Result<CommitId[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_signature_presence", { repoId, commitIds }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Files changed by a commit, relative to its first parent (empty tree for a
 * root commit). Drives the Changed Files panel.
 */
async repoCommitFiles(repoId: string, commitId: string) : Promise<Result<CommitFileChange[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_commit_files", { repoId, commitId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Files changed between two arbitrary revs — the Compare view's file list.
 */
async repoDiffFiles(repoId: string, from: string, to: string) : Promise<Result<CommitFileChange[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_diff_files", { repoId, from, to }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoSearchCommits(repoId: string, query: string, kind: CommitSearchKind, maxCount: number) : Promise<Result<Commit[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_search_commits", { repoId, query, kind, maxCount }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Resolve any rev-parse expression (SHA prefix, branch, tag, `HEAD~2`, ...)
 * to the commit it names, peeling tags. Backs the Commits panel's "Go to".
 */
async repoResolveCommit(repoId: string, rev: string) : Promise<Result<CommitId, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_resolve_commit", { repoId, rev }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoSearchPaths(repoId: string, query: string, maxCount: number) : Promise<Result<string[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_search_paths", { repoId, query, maxCount }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Every file in the repo, classified tracked/untracked/(ignored). Backs the
 * Files tree.
 */
async repoListFiles(repoId: string, showIgnored: boolean) : Promise<Result<RepoFileEntry[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_list_files", { repoId, showIgnored }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Every entry in the tree of `rev` (browse-at-commit mode of the Files
 * panel). A commit only records tracked content, so every entry is
 * `Tracked`; gitlinks carry the `submodule` flag.
 */
async repoFilesAtRevision(repoId: string, rev: string) : Promise<Result<RepoFileEntry[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_files_at_revision", { repoId, rev }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Append a path to the repo-root `.gitignore` (`path` for a file, `path/` for
 * a directory), anchored to the repo root. A no-op if the exact line is
 * already present.
 */
async repoAddToGitignore(repoId: string, path: string, isDir: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_add_to_gitignore", { repoId, path, isDir }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Stop tracking a file (`git rm --cached`, keeps it on disk) and add it to
 * `.gitignore`. If the git step succeeds but the gitignore write fails, the
 * error says so - the file *is* already untracked.
 */
async repoUntrackPath(repoId: string, path: string, isDir: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_untrack_path", { repoId, path, isDir }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Open the OS file manager with `path` revealed/selected (best effort).
 */
async repoRevealPath(repoId: string, path: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_reveal_path", { repoId, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * A file's current working-tree content (binary-aware), for File View's
 * working-tree mode. Mirrors `file_at_revision`'s classification but reads the
 * file from disk rather than `git show <rev>:<path>`, so it works for
 * untracked and ignored files too.
 */
async repoFileWorktree(repoId: string, path: string) : Promise<Result<FileAtRevision, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_file_worktree", { repoId, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Blame `path` - at `rev` when given, else the working tree.
 */
async repoBlame(repoId: string, path: string, rev: string | null) : Promise<Result<BlameHunk[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_blame", { repoId, path, rev }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Merge base of two revs, or null for unrelated histories (Compare's
 * three-dot mode diffs from this base instead of `from` itself).
 */
async repoMergeBase(repoId: string, a: string, b: string) : Promise<Result<string | null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_merge_base", { repoId, a, b }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Full content of a repo-relative file as of an arbitrary tree-ish; binary
 * content is classified (with its blob size) instead of returned.
 */
async repoFileAtRevision(repoId: string, rev: string, path: string) : Promise<Result<FileAtRevision, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_file_at_revision", { repoId, rev, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * A single file's commit history (newest first), following renames. `path`
 * is repo-relative; `max_count`/`skip` page the walk. `start_rev` walks
 * from that revision instead of HEAD (browse-at-commit mode).
 */
async repoFileHistory(repoId: string, path: string, maxCount: number, skip: number, startRev: string | null) : Promise<Result<FileHistoryEntry[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_file_history", { repoId, path, maxCount, skip, startRev }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Restore a single file (index + working tree) to its content at `rev`.
 * Destructive - git overwrites local changes to the file without complaint;
 * the destructive-confirmation gate lives in the UI.
 */
async repoRestoreFileAtRevision(repoId: string, rev: string, path: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_restore_file_at_revision", { repoId, rev, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * The diff for `path` from one of the comparison sources (working unstaged,
 * working staged, or a commit vs its parent). `context` is the number of
 * surrounding context lines — small for the chunked view, very large for the
 * whole-file view.
 */
async repoDiff(repoId: string, source: DiffSource, path: string, oldPath: string | null, context: number) : Promise<Result<DiffEntry, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_diff", { repoId, source, path, oldPath, context }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Stage a single hunk of `path`'s unstaged diff (`git apply --cached`).
 */
async repoStageHunk(repoId: string, path: string, hunkIndex: number) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_stage_hunk", { repoId, path, hunkIndex }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Unstage a single hunk of `path`'s staged diff (`git apply --cached -R`).
 */
async repoUnstageHunk(repoId: string, path: string, hunkIndex: number) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_unstage_hunk", { repoId, path, hunkIndex }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Discard a single hunk of `path`'s unstaged diff from the working tree
 * (`git apply -R`).
 */
async repoDiscardHunk(repoId: string, path: string, hunkIndex: number) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_discard_hunk", { repoId, path, hunkIndex }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Stage a subset of a hunk's lines (`line_indices` index into the hunk's diff
 * lines, context included).
 */
async repoStageLines(repoId: string, path: string, hunkIndex: number, lineIndices: number[]) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_stage_lines", { repoId, path, hunkIndex, lineIndices }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Unstage a subset of a hunk's lines.
 */
async repoUnstageLines(repoId: string, path: string, hunkIndex: number, lineIndices: number[]) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_unstage_lines", { repoId, path, hunkIndex, lineIndices }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Discard a subset of a hunk's lines from the working tree.
 */
async repoDiscardLines(repoId: string, path: string, hunkIndex: number, lineIndices: number[]) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_discard_lines", { repoId, path, hunkIndex, lineIndices }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Stage the given paths (`git add`).
 */
async repoStage(repoId: string, paths: string[]) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_stage", { repoId, paths }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Unstage the given paths (`git restore --staged`).
 */
async repoUnstage(repoId: string, paths: string[]) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_unstage", { repoId, paths }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Discard working-tree changes for the given paths (revert tracked, remove untracked).
 */
async repoDiscard(repoId: string, paths: string[]) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_discard", { repoId, paths }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Read a working-tree file as UTF-8 text (the editable diff's save baseline).
 */
async repoReadWorktreeFile(repoId: string, path: string) : Promise<Result<string, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_read_worktree_file", { repoId, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Overwrite a working-tree file with the given text (the editable diff's
 * save path). The frontend is responsible for EOL preservation.
 */
async repoWriteWorktreeFile(repoId: string, path: string, content: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_write_worktree_file", { repoId, path, content }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoMerge(repoId: string, target: string, options: MergeOptions) : Promise<Result<MergeOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_merge", { repoId, target, options }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoMergeContinue(repoId: string) : Promise<Result<MergeOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_merge_continue", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoMergeAbort(repoId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_merge_abort", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoRebase(repoId: string, onto: string) : Promise<Result<RebaseOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_rebase", { repoId, onto }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoRebaseContinue(repoId: string) : Promise<Result<RebaseOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_rebase_continue", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoRebaseSkip(repoId: string) : Promise<Result<RebaseOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_rebase_skip", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoRebaseAbort(repoId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_rebase_abort", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoRebaseInteractive(repoId: string, base: string, plan: RebaseStep[]) : Promise<Result<RebaseOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_rebase_interactive", { repoId, base, plan }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Probe behind the interactive-rebase panel's pushed-commit chips and
 * transplant notice (which range commits are not on the upstream; whether
 * the base is an ancestor of HEAD).
 */
async repoRebaseRangeInfo(repoId: string, base: string) : Promise<Result<RebaseRangeInfo, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_rebase_range_info", { repoId, base }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Commits reachable from HEAD but on NO remote-tracking ref (newest first,
 * capped). Backs the bulk drop/squash gate.
 */
async repoUnpushedCommits(repoId: string, maxCount: number) : Promise<Result<CommitId[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_unpushed_commits", { repoId, maxCount }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoConflictFileSides(repoId: string, path: string) : Promise<Result<ConflictFileSides, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_conflict_file_sides", { repoId, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoReset(repoId: string, target: string, mode: ResetMode) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_reset", { repoId, target, mode }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoRevert(repoId: string, shas: string[], mainline: number | null) : Promise<Result<SequenceOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_revert", { repoId, shas, mainline }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoCherryPick(repoId: string, shas: string[], mainline: number | null) : Promise<Result<SequenceOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_cherry_pick", { repoId, shas, mainline }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoCherryPickContinue(repoId: string) : Promise<Result<SequenceOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_cherry_pick_continue", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoCherryPickSkip(repoId: string) : Promise<Result<SequenceOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_cherry_pick_skip", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoCherryPickAbort(repoId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_cherry_pick_abort", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoRevertContinue(repoId: string) : Promise<Result<SequenceOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_revert_continue", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoRevertSkip(repoId: string) : Promise<Result<SequenceOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_revert_skip", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoRevertAbort(repoId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_revert_abort", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoReflog(repoId: string, maxCount: number) : Promise<Result<ReflogEntry[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_reflog", { repoId, maxCount }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoOpState(repoId: string) : Promise<Result<RepoOpState, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_op_state", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoConflictEntries(repoId: string) : Promise<Result<ConflictEntry[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_conflict_entries", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoResolveTakeSide(repoId: string, path: string, side: ConflictSide) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_resolve_take_side", { repoId, path, side }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoResolveUndoPaths(repoId: string) : Promise<Result<string[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_resolve_undo_paths", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoStagedMarkerPaths(repoId: string) : Promise<Result<string[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_staged_marker_paths", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoUnstagedMarkerPaths(repoId: string) : Promise<Result<string[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_unstaged_marker_paths", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoConflictReopen(repoId: string, path: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_conflict_reopen", { repoId, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Commit the staged changes with the given message; returns the new commit id.
 * When `amend` is set, rewrites HEAD instead of creating a new commit.
 */
async repoCommit(repoId: string, message: string, amend: boolean) : Promise<Result<CommitId, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_commit", { repoId, message, amend }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Pre-commit consistency check: staged `.gitmodules` vs staged gitlinks.
 * Findings feed the composer's warning banner; empty = nothing to warn about.
 */
async repoGitmodulesConsistency(repoId: string) : Promise<Result<GitmodulesFinding[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_gitmodules_consistency", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Case-only rename drift: tracked paths whose on-disk case differs from the
 * index - invisible to `git status` on a case-insensitive filesystem. Empty
 * when `core.ignorecase` is false/unset.
 */
async repoCaseDrift(repoId: string) : Promise<Result<CaseDriftEntry[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_case_drift", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Stage a case-only rename (`git mv`), normalizing the on-disk case too.
 */
async repoStageCaseRename(repoId: string, from: string, to: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_stage_case_rename", { repoId, from, to }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Discard detected case drift: rename the disk file back to the tracked
 * spelling, leaving the index untouched.
 */
async repoDiscardCaseRename(repoId: string, indexPath: string, diskPath: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_discard_case_rename", { repoId, indexPath, diskPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * All worktrees of the repo, main first.
 */
async repoWorktreeList(repoId: string) : Promise<Result<WorktreeInfo[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_worktree_list", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Create a worktree at `path` (absolute, on the repo's host).
 */
async repoWorktreeAdd(repoId: string, path: string, mode: WorktreeAddMode) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_worktree_add", { repoId, path, mode }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Remove a worktree checkout; `force` also drops a dirty tree.
 */
async repoWorktreeRemove(repoId: string, path: string, force: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_worktree_remove", { repoId, path, force }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Drop stale bookkeeping of manually deleted worktrees.
 */
async repoWorktreePrune(repoId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_worktree_prune", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Protect a worktree from remove/prune, with an optional reason.
 */
async repoWorktreeLock(repoId: string, path: string, reason: string | null) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_worktree_lock", { repoId, path, reason }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async repoWorktreeUnlock(repoId: string, path: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_worktree_unlock", { repoId, path }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Reword (rename) a commit's message; returns the new commit id. v1 rewords
 * HEAD only and refuses commits already reachable from a remote (pushed).
 */
async repoRewordCommit(repoId: string, commitId: string, message: string) : Promise<Result<CommitId, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_reword_commit", { repoId, commitId, message }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Fetch from remote(s). Cancellable via `op_id` (see `console_cancel`).
 */
async repoFetch(repoId: string, opts: FetchOptions, opId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_fetch", { repoId, opts, opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Pull (fetch + integrate) for the current branch. Cancellable via `op_id`.
 */
async repoPull(repoId: string, opts: PullOptions, opId: string) : Promise<Result<PullOutcome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_pull", { repoId, opts, opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Push the current branch to its remote. Cancellable via `op_id`.
 */
async repoPush(repoId: string, opts: PushOptions, opId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_push", { repoId, opts, opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Ahead/behind status of the current branch vs its upstream. `None` when HEAD
 * is detached or the current branch has no upstream configured.
 */
async repoTrackingStatus(repoId: string) : Promise<Result<TrackingStatus | null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_tracking_status", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * List the configured remotes with their fetch/push URLs.
 */
async repoListRemotes(repoId: string) : Promise<Result<Remote[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_list_remotes", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Add a remote (`git remote add`).
 */
async repoAddRemote(repoId: string, name: string, url: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_add_remote", { repoId, name, url }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Remove a remote and its tracking refs (`git remote remove`).
 */
async repoRemoveRemote(repoId: string, name: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_remove_remote", { repoId, name }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Rename a remote (`git remote rename`).
 */
async repoRenameRemote(repoId: string, oldName: string, newName: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_rename_remote", { repoId, oldName, newName }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Set a remote's fetch or push URL (`git remote set-url [--push]`).
 */
async repoSetRemoteUrl(repoId: string, name: string, url: string, push: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_set_remote_url", { repoId, name, url, push }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Prune stale remote-tracking refs (`git remote prune`). Cancellable via `op_id`.
 */
async repoPruneRemote(repoId: string, name: string, opId: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("repo_prune_remote", { repoId, name, opId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Return all lane locks for an open repo.
 */
async listLaneLocks(repoId: string) : Promise<Result<LaneLock[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_lane_locks", { repoId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Add or replace a lane lock. Returns the updated lock list.
 * Rejects lane_index > 64.
 */
async setLaneLock(repoId: string, refName: string, laneIndex: number) : Promise<Result<LaneLock[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_lane_lock", { repoId, refName, laneIndex }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Remove a lane lock by ref name. Returns the updated lock list (no-op if not found).
 */
async unsetLaneLock(repoId: string, refName: string) : Promise<Result<LaneLock[], AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("unset_lane_lock", { repoId, refName }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async saveColumnPreferences(prefs: JsonValue) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_column_preferences", { prefs }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Persist the Changed Files panel's view mode (`"tree"` | `"flat"`).
 */
async saveChangedFilesViewMode(mode: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_changed_files_view_mode", { mode }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Persist the Branches section's list style (`"tree"` | `"flat"`).
 */
async saveBranchListView(mode: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_branch_list_view", { mode }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Persist the Refs panel sort order for branches
 * (`"alphabetical"` | `"date"` | `"date_reversed"`). Unknown values are
 * stored as-is; the frontend falls back to alphabetical when reading.
 */
async saveRefsSortMode(mode: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_refs_sort_mode", { mode }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Persist the Tags section's own sort order (same values; unset inherits
 * `refs_sort_mode`).
 */
async saveTagsSortMode(mode: string) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_tags_sort_mode", { mode }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Whether creating a branch also checks it out (default true).
 */
async setCheckoutNewBranch(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_checkout_new_branch", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Persist the global UI font size (px), clamped to a sane range.
 */
async saveUiFontSize(size: number) : Promise<Result<number, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_ui_font_size", { size }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Persist the dock chrome dimensions: the gap between panel groups and the
 * groups' corner radius (px, clamped; 0/0 = the flush square default).
 */
async savePanelChrome(gap: number, radius: number, border: number) : Promise<Result<PanelChrome, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_panel_chrome", { gap, radius, border }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async saveLaneColoredBranchChips(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_lane_colored_branch_chips", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async saveStashBaseLaneColor(enabled: boolean) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_stash_base_lane_color", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Persist the Commits-panel graph metrics (row/line height, per-lane width,
 * commit-dot radius, and connector line width). Clamps each value to sane px
 * bounds before storing; the dot radius and line width are capped to half the
 * smaller cell dimension so they can never overflow the cell or overlap a
 * neighbouring lane. Text has no per-panel size — it follows the global UI
 * font size.
 */
async saveCommitsGraphMetrics(rowHeight: number, laneWidth: number, dotRadius: number, lineWidth: number) : Promise<Result<null, AppError>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("save_commits_graph_metrics", { rowHeight, laneWidth, dotRadius, lineWidth }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
}
}

/** user-defined events **/



/** user-defined constants **/



/** user-defined types **/

export type AppError = { kind: "UnknownRepo"; details: string } | { kind: "NotARepo"; details: string } | { kind: "Io"; details: string } | { kind: "Git"; details: GitError } | { kind: "GitUnavailable"; details: string } | { kind: "ForbiddenArg"; details: string } | { kind: "InvalidTheme"; details: string } | { kind: "InvalidLayout"; details: string } | { kind: "Settings"; details: string } | { kind: "ParseArgs"; details: string } | { kind: "InvalidLockIndex"; details: number } | { kind: "UnknownProfile"; details: string }
/**
 * A credential helper found on this machine.
 */
export type AvailableHelper = { 
/**
 * The value to write into `credential.helper` (e.g. `manager`).
 */
name: string; 
/**
 * Where the executable was found.
 */
path: string }
export type BinaryDiff = { old_path: string | null; new_path: string | null; old_size: number | null; new_size: number | null }
/**
 * One blame hunk: a run of consecutive lines last touched by the same
 * commit. Carries the line contents (git's porcelain output includes them),
 * so the Blame view needs no separate file read and can never misalign.
 */
export type BlameHunk = { 
/**
 * The blamed commit; all-zeros for uncommitted working-tree lines.
 */
sha: CommitId; author: string; 
/**
 * Author timestamp (Unix seconds).
 */
timestamp: number; 
/**
 * The commit's subject line.
 */
summary: string; 
/**
 * 1-based first line number in the blamed file.
 */
start_line: number; 
/**
 * The hunk's line contents, in file order.
 */
lines: string[]; 
/**
 * Whether the blamed commit has a prior version of the file (git's
 * porcelain `previous` header). False for the commit that introduced the
 * file (or a root commit): blaming its parent would fail, so the "blame
 * parent" affordance is hidden.
 */
has_previous: boolean; 
/**
 * The `previous` header's parent commit - the right revision to re-blame
 * at (git picks the correct parent for merges, unlike a naive `<sha>^`).
 */
previous_sha: CommitId | null; 
/**
 * The file's path AT `previous_sha` - the OLD name when the blamed
 * commit renamed the file. Re-blaming must use this path: the current
 * name does not exist in the parent after a rename.
 */
previous_path: string | null }
/**
 * A local or remote branch.
 */
export type Branch = { name: string; is_current: boolean; is_remote: boolean; upstream: string | null; head: CommitId | null; 
/**
 * Commits ahead of the upstream (`%(upstream:track)`); `None` when in
 * sync, without an upstream, or for remote branches.
 */
ahead: number | null; 
/**
 * Commits behind the upstream; `None` under the same conditions.
 */
behind: number | null; 
/**
 * The configured upstream ref no longer exists (`[gone]`).
 */
upstream_gone: boolean; 
/**
 * `%(creatordate:unix)`: the tip commit's committer date (Unix seconds).
 * Drives the user-selectable ref sort order; 0 when git returned none.
 */
created_at: number }
/**
 * Post-refusal analysis for a "not fully merged" branch delete: the signals
 * that let the UI say whether force-deleting is actually safe.
 */
export type BranchMergeAnalysis = { 
/**
 * Refs (short names) that already contain the branch tip - a true merge
 * landed and only the checked-out base is stale. The branch itself, its
 * remote counterparts, and remote HEAD symrefs are excluded.
 * (No literal star-slash here: specta copies this into a TS block
 * comment in bindings.ts, where it would terminate the comment.)
 */
merged_into: string[]; 
/**
 * Baseline ref (e.g. `origin/main`) whose history holds a patch-id
 * equivalent of every commit unique to the branch - the signature of a
 * squash/rebase merge. `None` when no baseline was found or commits
 * remain unmatched.
 */
equivalent_in: string | null }
/**
 * A tracked path whose on-disk spelling differs from the index only by
 * letter case. On a case-insensitive filesystem (`core.ignorecase=true`)
 * `git status` cannot see such a rename - the index path still resolves to
 * the renamed file - so it is detected by comparing index paths against
 * directory listings and fixed by staging the rename (`git mv`). A finding,
 * not an error (like `GitmodulesFinding`).
 */
export type CaseDriftEntry = { 
/**
 * Path as recorded in the index (repo-relative, '/'-separated).
 */
index_path: string; 
/**
 * The same path as actually spelled on disk.
 */
disk_path: string; 
/**
 * The drifting component is a directory: the fix renames the directory,
 * covering every tracked path under it (nested drift is collapsed to
 * the shallowest drifting component).
 */
is_dir: boolean }
/**
 * `repo_clone`'s result: the opened repo plus any LFS pointer stubs the
 * clone's checkout left behind (git can exit 0 with failed LFS downloads
 * under `lfs.skipdownloaderrors` / a non-required filter).
 */
export type CloneOutcome = { summary: RepoSummary; lfs_stubs: LfsStubs | null }
/**
 * A commit as exposed to the UI.
 */
export type Commit = { id: CommitId; parents: CommitId[]; author: Signature; committer: Signature; message: string; 
/**
 * Author timestamp (Unix seconds).
 */
timestamp: number; signature: SignatureVerification | null; 
/**
 * Presence-only: the raw commit object carries a signature header
 * (`gpgsig`/`gpgsig-sha256`/`mergetag`). Detected by the commit list's
 * batched header scan - NOT a verification result (`signature` is, and
 * stays on-demand; the bulk log must never verify, see LOG_FORMAT).
 */
has_signature: boolean; decorations?: RefDecoration[] }
/**
 * Default action of the Working Changes commit button: plain commit, or
 * commit followed by a push of the current branch. Deliberately per-repo
 * ONLY (no global default): push is outward-facing, and a globally
 * persisted Commit & Push would silently follow the user from a hobby repo
 * into one where every push triggers CI. The button's caret menu is the
 * only UI - there is deliberately no RepoSettingsPanel section (see the
 * BACKLOG "Commit & Push" entry).
 */
export type CommitButtonMode = "commit" | "commit_and_push"
/**
 * Absolute date format for the Commits panel's Date column (applies when
 * `commit_date_absolute` is on). Rendering lives in the frontend
 * (`src/lib/time.ts`); this only persists the choice.
 */
export type CommitDateFormat = 
/**
 * ISO 8601: 2026-07-20 14:30
 */
"iso" | 
/**
 * Swiss/German: 20.07.2026 14:30
 */
"swiss" | 
/**
 * UK/European: 20/07/2026 14:30
 */
"uk" | 
/**
 * US: 07/20/2026 2:30 PM
 */
"us"
/**
 * Detailed information for a single commit (Commit Details panel).
 * 
 * `commit.signature` is populated from `git verify-commit --raw` when the
 * commit object contains a `gpgsig` or similar header; otherwise it is `None`.
 * Verification is intentionally omitted from the log listing (too expensive
 * per row) and only computed on demand here.
 */
export type CommitDetails = { commit: Commit; 
/**
 * The raw output of `git cat-file -p <sha>` — available for power-user inspection.
 */
raw_object: string }
/**
 * A single file changed by a commit, relative to its first parent (or the
 * empty tree for a root commit). Backs the Changed Files panel.
 */
export type CommitFileChange = { 
/**
 * The file's path after the change (the destination path for renames/copies).
 */
path: string; 
/**
 * The source path for a rename or copy; `None` otherwise.
 */
old_path: string | null; change: FileState; 
/**
 * Added lines. `0` for binary files (see `binary`).
 */
additions: number; 
/**
 * Removed lines. `0` for binary files (see `binary`).
 */
deletions: number; 
/**
 * True when git reports the file as binary (numstat `-`/`-`).
 */
binary: boolean }
/**
 * SHA-1 (or SHA-256) hex string identifying a commit.
 */
export type CommitId = string
/**
 * What a commit search matches against. `Message`/`Author` are
 * case-insensitive regexes (git `--grep`/`--author`); `Content` is the
 * pickaxe (`-S`): commits that change the number of occurrences of the
 * literal string.
 */
export type CommitSearchKind = "message" | "author" | 
/**
 * Pickaxe (`-S`): commits changing the NUMBER of occurrences of the
 * literal string.
 */
"content" | 
/**
 * `-G`: commits whose diff has an added/removed line matching the regex.
 */
"content_regex"
export type ConfigScope = "local" | "global" | "system" | "unset"
export type ConfigValue = { 
/**
 * `None` means the key is not set at this scope.
 */
value: string | null; source: ConfigScope }
/**
 * A conflicted path and how it conflicts (`git ls-files -u`).
 */
export type ConflictEntry = { path: string; kind: ConflictKind }
/**
 * The three index stages of a conflicted path (1 = base, 2 = ours,
 * 3 = theirs), for the 3-way resolve view. A side is `None` when that stage
 * is absent: add/add conflicts have no base, delete conflicts lack one side.
 */
export type ConflictFileSides = { base: string | null; ours: string | null; theirs: string | null }
/**
 * How a path conflicts, from the index's unmerged stages
 * (1 = base, 2 = ours, 3 = theirs).
 */
export type ConflictKind = "both_modified" | "both_added" | "deleted_by_us" | "deleted_by_them"
/**
 * Which side of a conflict to take for a whole file.
 */
export type ConflictSide = "ours" | "theirs"
/**
 * One connected platform account (see `commands/accounts.rs`).
 */
export type ConnectedAccountMeta = { 
/**
 * Platform id: "github" | "gitlab" | "azure_devops".
 */
platform: string; 
/**
 * Git HTTPS host, which is also the keychain key host (e.g. "github.com").
 */
host: string; 
/**
 * Account username (doubles as the git basic-auth username).
 */
username: string; display_name: string | null }
/**
 * A connected account plus whether its token is still in the keychain: a
 * revoked token gets erased by git (the shim honors `erase`), which leaves
 * the metadata behind: the UI flags that as "reconnect needed".
 */
export type ConnectedAccountStatus = { account: ConnectedAccountMeta; token_present: boolean }
export type ConsoleExecHandle = { op_id: string; argv: string[] }
/**
 * The effective helper entry per scope (last non-empty entry at that scope;
 * `None` = no helper configured there). Local scope is deliberately absent:
 * this backs a global-settings editor (see `read_config_global_scopes` for
 * why global views must not consult local scope).
 */
export type CredentialHelperView = { helper_global: string | null; helper_system: string | null }
export type DiffEntry = { Text: TextDiff } | { Binary: BinaryDiff } | { Submodule: SubmoduleChange } | 
/**
 * The raw diff text exceeded the display cap. Rendering it would move
 * multiple copies of the content through IPC and the webview (which
 * crashed with renderer OOM on multi-MB single-line files); the panel
 * shows a notice instead. An outcome, not an error (house rule).
 */
{ TooLarge: { bytes: number } }
/**
 * A hunk in a textual diff.
 */
export type DiffHunk = { old_start: number; old_lines: number; new_start: number; new_lines: number; header: string; lines: DiffLine[] }
export type DiffLine = { kind: DiffLineKind; content: string }
export type DiffLineKind = "Context" | "Added" | "Removed"
/**
 * What two sides the Diff panel is comparing for a single file.
 */
export type DiffSource = 
/**
 * Index vs working tree (`git diff`).
 */
{ kind: "working_unstaged" } | 
/**
 * HEAD vs index (`git diff --cached`).
 */
{ kind: "working_staged" } | 
/**
 * A commit vs its first parent (`git diff <parent> <sha>`).
 */
{ kind: "commit"; commit_id: CommitId } | 
/**
 * Two arbitrary revs (`git diff <from> <to>`) — the Compare view. The
 * fields accept any rev spec (branch names, `HEAD~3`, shas), carried in
 * `CommitId` for IPC uniformity.
 */
{ kind: "commit_range"; from: CommitId; to: CommitId }
/**
 * What the optional local fast-forward step of `checkout_remote_branch` did.
 * The step is `git merge --ff-only <remote-ref>` against the already-fetched
 * remote-tracking ref - purely local, never a network pull. Every non-happy
 * case here is an outcome, not an error: the checkout itself succeeded.
 */
export type FastForwardResult = 
/**
 * Fast-forwarding was not requested (the setting is off).
 */
{ kind: "not_attempted" } | 
/**
 * The local branch was behind and now points at the remote tip.
 */
{ kind: "fast_forwarded" } | 
/**
 * Already at the remote tip - including a tracking branch that
 * `switch --track` just created there.
 */
{ kind: "up_to_date" } | 
/**
 * Local and remote have diverged: a fast-forward is impossible and the
 * local branch was left untouched.
 */
{ kind: "diverged" } | 
/**
 * The fast-forward failed for another reason (e.g. reapplied auto-stash
 * changes would be overwritten); the local branch was left untouched.
 */
{ kind: "failed"; message: string }
/**
 * Options for `git fetch`.
 */
export type FetchOptions = { 
/**
 * Fetch from all remotes (`--all`) rather than a single named remote.
 */
all: boolean; 
/**
 * Prune remote-tracking refs that no longer exist on the remote (`--prune`).
 */
prune: boolean; 
/**
 * The remote to fetch when `all` is false; ignored when `all` is true.
 */
remote: string | null }
/**
 * Fast-forward behavior for `merge`.
 */
export type FfMode = 
/**
 * git's default: fast-forward when possible, else a merge commit.
 */
"auto" | "no_ff" | "ff_only"
/**
 * A file's content at a revision: text, or a binary classification (NUL
 * sniff, git's own heuristic) with the blob's exact size so the UI can say
 * "binary file, N bytes" instead of rendering mojibake.
 */
export type FileAtRevision = { Text: string } | { Binary: { size_bytes: number } }
/**
 * One commit in a single file's history (`git log --follow`), with the
 * file's path AS OF THAT COMMIT - pre-rename commits carry the old name, so
 * "view/blame/diff at this commit" can address the file that actually
 * existed there.
 */
export type FileHistoryEntry = { commit_id: CommitId; summary: string; author: string; 
/**
 * Author date, unix seconds.
 */
timestamp: number; 
/**
 * The file's repo-relative path at this commit.
 */
path: string; 
/**
 * Set only on the commit that renamed the file (its previous name).
 */
old_path: string | null }
/**
 * What a preview request resolved to. `Absent` covers unresolvable specs
 * (deleted side, root commit's `^`), so the UI renders added/removed.
 */
export type FilePreview = { kind: "image"; format: ImageFormat; size: number; base64: string } | { kind: "too_large"; size: number } | { kind: "not_previewable"; size: number } | { kind: "absent" } | { kind: "lfs_missing"; oid: string; size: number }
/**
 * Working-tree / index state of a single path.
 */
export type FileState = "Modified" | "Added" | "Deleted" | "Renamed" | "Copied" | "Untracked" | "Ignored" | "Conflicted" | 
/**
 * A submodule whose recorded pointer moved (staged or working-tree) - a
 * real, committable superproject change.
 */
"SubmoduleChanged" | 
/**
 * A submodule with uncommitted changes INSIDE its worktree but an
 * unmoved pointer. Informational: nothing here is stageable or
 * committable from the superproject - the changes live in the
 * submodule's own repo.
 */
"SubmoduleDirty"
export type FileStatus = { path: string; state: FileState; 
/**
 * Whether the change is staged (in the index). `false` for working-tree-only changes.
 */
staged: boolean; 
/**
 * Added lines for this entry's own diff (index diff when staged, worktree
 * diff when not). `None` when git reports no counts for the path
 * (untracked, conflicted, binary) — distinct from a genuine `0`.
 */
additions: number | null; 
/**
 * Removed lines; same semantics as `additions`.
 */
deletions: number | null; 
/**
 * True when git reports the file as binary (numstat `-`/`-`).
 */
binary: boolean; 
/**
 * Rename/copy source path, set only on `Renamed`/`Copied` entries. The
 * diff of such an entry must pair both sides (`--find-renames -- old
 * new`), or it reads as a whole-file add/delete.
 */
old_path: string | null }
export type GitAttrRule = { pattern: string; 
/**
 * `text` attribute value: "set", "auto", or "unset" (from `-text`/`binary`).
 */
text: string | null; 
/**
 * `eol` attribute value: "lf" or "crlf".
 */
eol: string | null }
export type GitError = { kind: "RefNotFound"; details: string } | { kind: "AuthFailed"; details: string } | { kind: "PushRejected"; details: { stderr: string } } | { kind: "UnpushedSubmodules"; details: { stderr: string } } | { kind: "WouldOverwriteLocalChanges"; details: string } | 
/**
 * `git branch -d` refused because the branch is not fully merged (its
 * tip is not reachable from the upstream / HEAD). The UI reacts with a
 * guided force-delete offer instead of a raw error.
 */
{ kind: "BranchNotFullyMerged"; details: { branch: string; stderr: string } } | 
/**
 * A branch operation was refused because the branch is checked out in
 * another worktree (git allows a branch in only one worktree at a
 * time). `branch`/`path` are best-effort extractions from git's
 * message so the UI can name (and offer to open) that worktree.
 */
{ kind: "CheckedOutInWorktree"; details: { branch: string | null; path: string | null; stderr: string } } | 
/**
 * A git-lfs smudge/download failure inside an operation (pull, switch,
 * clone, ...): the affected worktree paths hold no real content. When
 * `missing_on_remote`, the objects were never uploaded (whoever pushed
 * the commit must `git lfs push`); otherwise the download itself failed
 * (network/auth).
 */
{ kind: "LfsDownloadFailed"; details: { files: string[]; missing_on_remote: boolean; stderr: string } } | { kind: "CommandFailed"; details: { exit_code: number; stderr: string } } | 
/**
 * A clone the user cancelled: an expected outcome the UI stays silent
 * about - unless removing the partial clone's files failed, in which
 * case `cleanup_failed` carries the note the user must see (a failed
 * best-effort cleanup is never silent).
 */
{ kind: "CloneCancelled"; details: { cleanup_failed: string | null } } | 
/**
 * A ref / rev / remote name that would land in a POSITIONAL argv slot
 * while looking like a command-line option (it starts with `-`). Refused
 * before git runs: see `cli_impl::safe_ref` for why such a name can
 * reach us from a repository at all, and what it would do.
 * The payload is the user-facing sentence.
 */
{ kind: "UnsafeArgument"; details: string } | { kind: "Parse"; details: string } | { kind: "GitUnavailable"; details: string } | { kind: "RewordNotHead" } | { kind: "RewordPushed" } | { kind: "Internal"; details: string }
/**
 * A named git-identity profile, defined once globally and selectable per repo.
 * Bundles identity + signing + auth-key config that is written to a repo's
 * LOCAL `.git/config` when applied. Definitions live in `GlobalSettings`; the
 * per-repo selection lives in `RepoSettings::git_profile_id`.
 * 
 * Fields are `Option<String>`: `Some` sets the key on apply, `None` unsets it
 * (a profile is a *complete* identity — see `commands/profiles.rs`).
 */
export type GitProfile = { 
/**
 * Stable opaque id (UUID v4); survives renames and field edits.
 */
id: string; 
/**
 * Human label shown in the UI.
 */
name: string; userName: string | null; userEmail: string | null; gpgFormat: string | null; signingKey: string | null; commitGpgsign: string | null; allowedSignersFile: string | null; 
/**
 * Path to the auth SSH private key; synthesized into `core.sshCommand`
 * on apply and parsed back on read (see `commands/profiles.rs`).
 */
authSshKey: string | null; 
/**
 * HTTPS credential helper (e.g. `manager`, `store`, `osxkeychain`), written
 * to local `credential.helper` on apply. LeGit stores no secrets — the
 * helper holds them (see `commands/profiles.rs`).
 */
credentialHelper: string | null }
/**
 * Versioned on-disk envelope for the profile list (mirrors `LaneLocksDoc`).
 */
export type GitProfilesDoc = { format: string; formatVersion: number; profiles: GitProfile[] }
export type GitStatus = { resolved_path: string; 
/**
 * `null` when the binary cannot be spawned at all.
 */
version: GitVersion | null; 
/**
 * `true` when `version` is set and meets the minimum (§7.6).
 */
meets_minimum: boolean; minimum_required: [number, number, number]; user_override: string | null; error: string | null }
/**
 * Parsed `git --version` output.
 */
export type GitVersion = { raw: string; major: number; minor: number; patch: number }
/**
 * One inconsistency between the STAGED `.gitmodules` blob and the staged
 * gitlinks - i.e. what the next commit would record. Surfaced as a
 * pre-commit warning, never a block: git itself permits these states, but
 * committing one breaks consumers (`submodule init`, clone recursion,
 * `push --recurse-submodules`).
 */
export type GitmodulesFinding = 
/**
 * A `.gitmodules` section whose `path` has no gitlink in the index
 * (an empty `path` means the section has no usable path at all).
 */
{ kind: "entry_without_gitlink"; name: string; path: string } | 
/**
 * An indexed gitlink that no `.gitmodules` section points at.
 */
{ kind: "gitlink_without_entry"; path: string }
/**
 * User-level settings shared across all repos. On disk as
 * `<app-data>/global-settings.json`. See DESIGN-v0.2.md §G.
 */
export type GlobalSettings = { 
/**
 * Global default git binary path override (None = auto-detect).
 */
git_path_override: string | null; 
/**
 * Recent-repo history (deduplicated, newest first). Survives close.
 */
last_open_repos?: string[]; 
/**
 * Repos that were open at last shutdown; re-opened on launch.
 */
currently_open?: string[]; 
/**
 * Parent directory of the most recent successful clone. Prefills the
 * clone (and init) dialog's folder field - most users keep one source
 * directory where all their repositories live. None = never cloned.
 */
last_clone_parent_dir?: string | null; 
/**
 * Canonical path of the repo active at last shutdown.
 */
active_open_repo: string | null; 
/**
 * Name of the active theme.
 */
active_theme: string | null; 
/**
 * Whether the global region sits above (top) or to the left (left) of the
 * repo region.
 */
global_region_placement: RegionPlacement; 
/**
 * Divider size in px when in top mode; None = use default.
 */
global_region_size_top: number | null; 
/**
 * Divider size in px when in left mode; None = use default.
 */
global_region_size_left: number | null; 
/**
 * Whether the global dock is collapsed to zero height/width.
 */
global_dock_collapsed: boolean; 
/**
 * Attention-only line-ending chips on Working Changes rows; per-repo
 * settings can override.
 */
line_ending_chips_in_changes?: boolean; 
/**
 * Warn before committing staged line-ending changes; per-repo override.
 */
warn_on_line_ending_commit?: boolean; 
/**
 * Serialized column preferences for the Commits panel (frontend-owned schema).
 */
column_preferences?: JsonValue; 
/**
 * Line/row height for the Commits panel rows, in px.
 */
commits_row_height?: number; 
/**
 * Per-lane horizontal spacing for the Commits graph column, in px.
 */
commits_lane_width?: number; 
/**
 * Commit-dot radius for the Commits graph, in px.
 */
commits_dot_radius?: number; 
/**
 * Stroke width for the Commits graph connector lines/arcs, in px.
 */
commits_line_width?: number; 
/**
 * Remembered view mode for the Changed Files panel (`"tree"` | `"flat"`).
 * `None` until the user first toggles it.
 */
changed_files_view_mode?: string | null; 
/**
 * Branches section list style (`"tree"` | `"flat"`). `None`/unknown =
 * flat. Toggled from the Branches section header, applies globally.
 */
branch_list_view?: string | null; 
/**
 * Sort order for branches in the Refs panel
 * (`"alphabetical"` | `"date"` | `"date_reversed"`). `None` = alphabetical.
 * Tags inherit this unless `tags_sort_mode` is set.
 */
refs_sort_mode?: string | null; 
/**
 * Sort order for the Tags section. `None` = inherit `refs_sort_mode`.
 */
tags_sort_mode?: string | null; 
/**
 * Global UI font size (px). Base for every panel's text scale and for the
 * panel min-size constraints.
 */
ui_font_size?: number; 
/**
 * Gap between dockview panel groups (px); 0 = the compact flush look.
 */
panel_gap?: number; 
/**
 * Corner radius of dockview panel groups (px); 0 = square.
 */
panel_corner_radius?: number; 
/**
 * Border thickness of dockview panel groups (px) while the spaced/rounded
 * chrome is armed; 1 = the classic hairline. Any non-default value arms
 * the chrome even at 0 gap/radius.
 */
panel_border_width?: number; 
/**
 * Colour branch chips in the Commits graph from their row's lane instead
 * of the theme's static ref tokens (the theme contributes the per-part
 * filters via `laneChipFilters`).
 */
lane_colored_branch_chips?: boolean; 
/**
 * Colour a stash node with the lane of its BASE commit instead of the
 * lane the stash row occupies.
 */
stash_base_lane_color?: boolean; 
/**
 * Whether the filesystem watcher auto-refreshes the UI on disk changes.
 * When off, refresh falls back to window/panel focus only.
 */
watcher_enabled?: boolean; 
/**
 * Whether discarding changes asks for confirmation first. When off, discard
 * actions run immediately.
 */
confirm_discard?: boolean; 
/**
 * Detect case-only renames git status cannot see (tracked path vs
 * on-disk case on case-insensitive filesystems) and offer to stage
 * them. When off, the scan never runs.
 */
detect_case_renames?: boolean; 
/**
 * Whether creating a branch also checks it out (default true). Applies
 * to the Commits panel's inline create and the Branches section's form;
 * "branch from stash" always checks out (`git stash branch` semantics).
 */
checkout_new_branch?: boolean; 
/**
 * Periodic background auto-fetch of the active repo's remotes. OFF by
 * default (network access on a timer is opt-in). Fetch-only and quiet:
 * never pulls/merges, never toasts.
 */
auto_fetch_enabled?: boolean; 
/**
 * Minutes between background auto-fetches (UI enforces a minimum of 1).
 */
auto_fetch_interval_minutes?: number; 
/**
 * Check for app updates once at startup (quiet: only an update-available
 * toast; never downloads without consent). ON by default - check-only.
 */
check_updates_on_startup?: boolean; 
/**
 * Command template for "open in external editor" (e.g. `code "$ROOT"`);
 * `$ROOT` is the repo root, appended when absent. `None`/blank = no
 * editor configured — the action opens the folder in the OS file manager.
 */
external_editor_command?: string | null; 
/**
 * How to handle uncommitted changes when switching branches. `None` = `TryDirectly`.
 */
switch_dirty_behavior: SwitchDirtyBehavior | null; 
/**
 * Checking out a remote branch also fast-forwards the local branch to
 * the remote tip (a LOCAL `merge --ff-only`, never a network pull).
 * Default ON - landing on a stale local branch after double-clicking a
 * remote chip is the confusing behavior this setting exists to fix; off
 * = plain checkout like other git clients.
 */
checkout_remote_fast_forward?: boolean; 
/**
 * Pull integration strategy for the sync toolbar. `None` = `Default`
 * (the repo's `pull.rebase` config decides).
 */
pull_strategy?: PullStrategy | null; 
/**
 * Whether the Commits-toolbar Stash button includes untracked files.
 * Persisted default picked via the button's caret menu (pull-strategy
 * style); off = tracked changes only.
 */
stash_include_untracked?: boolean; 
/**
 * `git push --recurse-submodules` guard mode. `None` = off (no flag).
 */
push_recurse_submodules?: PushRecurseMode | null; 
/**
 * Auto-push tags with their commit (default OFF - tag pushes commonly
 * trigger CI release pipelines, so this is a deliberate opt-in): a push
 * also pushes the tags whose target commit became public through it, and
 * a tag created on an already-public commit is pushed immediately.
 * Repo-overridable (`RepoSettings::auto_push_tags`).
 */
auto_push_tags?: boolean; 
/**
 * After LeGit-driven submodule updates, attach a detached submodule HEAD
 * to a branch pointing at the same commit (configured branch first, else
 * a unique local match). Opt-in; see
 * design/2026-07-23-submodule-branch-attach.md.
 */
submodule_attach_branch?: boolean; 
/**
 * Show author avatars (Gravatar) in the commit graph. OFF by default —
 * enabling it sends hashed author emails to gravatar.com (privacy
 * opt-in; see BACKLOG/settings copy).
 */
commit_avatars?: boolean; 
/**
 * Show author initials in the commit graph dots (default off). Purely
 * local. With `commit_avatars` also on, initials show only for authors
 * without a Gravatar.
 */
commit_initials?: boolean; 
/**
 * Syntax-highlight code in the diff viewer. Off by default: highlighting
 * parses each hunk's sides on the UI thread, so it is opt-in.
 */
diff_syntax_highlighting?: boolean; 
/**
 * Show the full author datetime in the Commits panel's Date column
 * instead of the compact relative form (default off = relative).
 */
commit_date_absolute?: boolean; 
/**
 * Which absolute format the Date column uses (when `commit_date_absolute`).
 */
commit_date_format?: CommitDateFormat; 
/**
 * Whether the absolute Date column includes the time of day (on by
 * default; off shows the date only). Ignored while the column is relative.
 */
commit_date_show_time?: boolean; 
/**
 * Panel IDs the user has opted out of auto-opening: a `summon` to one of
 * these degrades to `notifyIfOpen` (updates it only if already open, never
 * pops it open). Empty = every panel auto-opens as before.
 */
suppressed_auto_open_panels?: string[]; 
/**
 * Top-to-bottom order of the Working Changes panel's three sections
 * ("unstaged", "staged", "commit"). Empty/partial is normalized on the UI
 * side; default is unstaged → staged → commit.
 */
working_changes_section_order?: string[]; 
/**
 * User-defined git identity profiles (versioned envelope).
 */
gitProfiles?: GitProfilesDoc; 
/**
 * Platform accounts connected via PAT (`commands/accounts.rs`).
 * METADATA ONLY: the token lives in the OS keychain under the broker's
 * `https://<host>` key; settings files hold no secrets.
 */
connected_accounts?: ConnectedAccountMeta[] }
/**
 * The host part of a locator, as it crosses IPC in `RepoSummary`
 * (`None`/absent = local). Hand-mirrored in `src/lib/types.ts`.
 */
export type HostRef = { kind: "wsl"; distro: string }
/**
 * `user.name` / `user.email` at global and system scope, plus the value git
 * resolves outside any repo (global > system). No local scope: this backs a
 * global-settings editor, which exists independently of any open repo.
 */
export type IdentityView = { name_global: ConfigValue; name_system: ConfigValue; name_resolved: ConfigValue; email_global: ConfigValue; email_system: ConfigValue; email_resolved: ConfigValue }
export type ImageFormat = "png" | "jpeg" | "gif" | "webp" | "bmp" | "ico" | 
/**
 * Extension-triggered (SVG is text - no magic bytes to sniff); the
 * webview renders it via an `<img>` data URL, where scripts never run.
 */
"svg"
export type JsonValue = null | boolean | number | string | JsonValue[] | Partial<{ [key in string]: JsonValue }>
/**
 * One differing key between the repo's local config and a profile.
 */
export type KeyDiff = { key: string; local: string | null; profile: string | null }
/**
 * Opaque identifier for a signing key (GPG fingerprint, SSH key path, ...).
 */
export type KeyId = string
export type KeybindingsFile = { version: number; 
/**
 * CommandId -> chords. `[]` = explicitly unbound; an absent id inherits
 * the shipped default (the frontend owns that semantics).
 */
bindings: Partial<{ [key in string]: string[] }> }
/**
 * A per-repo lane lock: pins a ref to a specific lane index.
 */
export type LaneLock = { refName: string; laneIndex: number }
/**
 * Versioned on-disk envelope for lane locks (§I.2).
 */
export type LaneLocksDoc = { format: string; formatVersion: number; locks: LaneLock[] }
export type LayoutEntry = { name: string; path: string }
/**
 * LFS patterns of the repo: the manageable root `.gitattributes` ones plus
 * (read-only) which nested attribute files also declare `filter=lfs`.
 */
export type LfsPatternsView = { root_patterns: string[]; nested_files: string[] }
/**
 * Repo LFS probe. `uses_lfs` comes from tracked `.gitattributes`
 * (`filter=lfs`); the binary/config probes run only when it is true
 * (false means installed/version/initialized report false/None unprobed).
 */
export type LfsStatus = { 
/**
 * Any tracked `.gitattributes` contains `filter=lfs`.
 */
uses_lfs: boolean; 
/**
 * `git lfs version` succeeded.
 */
installed: boolean; 
/**
 * First line of `git lfs version` stdout (e.g. `git-lfs/3.4.1 (...)`).
 */
version: string | null; 
/**
 * `filter.lfs.smudge` is set in git config (repo context, so local
 * config counts) - i.e. `git lfs install` has been run.
 */
initialized: boolean }
/**
 * A git-lfs download failure's residue in the worktree: paths left as
 * pointer stubs instead of real content. git can exit 0 in this state
 * (`lfs.skipdownloaderrors`, non-required filter), so operations report it
 * as data - the user must learn the files hold no real content, never a
 * silent success.
 */
export type LfsStubs = { files: string[]; 
/**
 * The objects are absent on the server (missing upload), not a
 * network/auth problem.
 */
missing_on_remote: boolean }
/**
 * The line-ending style of a file's (or blob's) text — the small indicator
 * shown in the Diff / File View / Blame panels.
 */
export type LineEndingKind = 
/**
 * Only `\n`.
 */
"lf" | 
/**
 * Only `\r\n`.
 */
"crlf" | 
/**
 * Only lone `\r`.
 */
"cr" | 
/**
 * A mix of more than one of the above.
 */
"mixed" | 
/**
 * No line breaks at all (empty or single line).
 */
"none" | 
/**
 * Binary content — no meaningful line endings.
 */
"binary"
/**
 * Line-ending summary for one changed file (`repo_line_ending_status`).
 * `unstaged` compares the index against what `git add` would store
 * (check-in normalization applied - policy-aware, so autocrlf conversions
 * are not flagged); `staged` compares HEAD against the index: exactly what
 * a commit records, immune to autocrlf by construction.
 */
export type LineEndingStatusEntry = { path: string; unstaged: LineEndingTransition | null; staged: LineEndingTransition | null; 
/**
 * The working file has mixed CRLF+LF endings.
 */
mixed: boolean; 
/**
 * Raw on-disk kind of the working file - the chip label never lies
 * about disk state. `None` when there is no readable working side.
 */
working_raw: LineEndingKind | null }
/**
 * A line-ending change between two sides of a changed file (old -> new).
 * Backs the Working Changes chips and the commit warning.
 */
export type LineEndingTransition = { from: LineEndingKind; to: LineEndingKind }
export type LineEndingsView = { autocrlf_local: ConfigValue; autocrlf_global: ConfigValue; autocrlf_system: ConfigValue; autocrlf_resolved: ConfigValue; eol_local: ConfigValue; eol_global: ConfigValue; eol_system: ConfigValue; eol_resolved: ConfigValue; 
/**
 * Line-ending-relevant rules from `.gitattributes`.
 */
gitattributes: GitAttrRule[]; 
/**
 * True when a `.gitattributes` rule covers all files (`*` pattern with a
 * text attribute) — in which case `core.autocrlf`/`eol` have no effect
 * for those files.
 */
gitattributes_covers_all: boolean }
/**
 * The repo section's data: live LOCAL values plus what the repo would
 * inherit without them (global scope, falling back to system).
 */
export type ManagedConfigView = { local: ManagedKeys; inherited: ManagedKeys }
/**
 * The eight managed git-config keys, projected to plain values. For the auth
 * key this holds the *key path* (parsed out of `core.sshCommand`), not the
 * full command.
 */
export type ManagedKeys = { user_name: string | null; user_email: string | null; gpg_format: string | null; signing_key: string | null; commit_gpgsign: string | null; allowed_signers_file: string | null; auth_ssh_key: string | null; credential_helper: string | null }
/**
 * Options for `merge`. `squash: true` ignores `ff` (git `--squash` never
 * creates a commit); the UI's menu items never combine them.
 */
export type MergeOptions = { ff: FfMode; squash: boolean }
/**
 * Outcome of `merge` / `merge_continue`. Conflicts are an outcome, not an
 * error: the merge is in progress and the user resolves + continues/aborts.
 */
export type MergeOutcome = { kind: "fast_forwarded" } | { kind: "merged" } | 
/**
 * `--squash`: changes staged, no commit created; the user commits.
 */
{ kind: "squashed" } | { kind: "already_up_to_date" } | { kind: "conflicts"; message: string }
export type PanelChrome = { gap: number; radius: number; border: number }
/**
 * How the repo's live local config relates to the defined profiles.
 */
export type ProfileMatch = 
/**
 * No managed keys set locally - repo uses inherited (global) identity.
 */
{ kind: "inherit" } | 
/**
 * Local config exactly equals a profile's projection (the stored
 * `git_profile_id` only tiebreaks identical profiles).
 */
{ kind: "active"; profile_id: string } | 
/**
 * Local has managed values matching no profile: a deliberate (or
 * externally made) repo-specific configuration.
 */
{ kind: "custom" }
export type ProfileStatus = { 
/**
 * Live LOCAL value of each managed key (None = unset locally).
 */
local: ManagedKeys; stored_profile_id: string | null; match: ProfileMatch }
/**
 * Options for `git pull`.
 */
export type PullOptions = { strategy: PullStrategy }
/**
 * Result of a successful `pull`: any LFS pointer stubs it left behind.
 */
export type PullOutcome = { lfs_stubs: LfsStubs | null }
/**
 * How `git pull` should integrate fetched changes.
 */
export type PullStrategy = 
/**
 * Honor the repo's `pull.rebase` config (pass no integration flag).
 */
"Default" | 
/**
 * `--rebase`.
 */
"Rebase" | 
/**
 * `--no-rebase` (merge).
 */
"Merge" | 
/**
 * `--ff-only`.
 */
"FfOnly"
/**
 * Options for `git push`. The remote and branch are always passed explicitly to
 * avoid `push.default` surprises.
 */
export type PushOptions = { remote: string; branch: string; 
/**
 * Set the pushed branch as upstream (`--set-upstream`) — used to publish a
 * branch that has no tracking configuration yet.
 */
set_upstream: boolean; 
/**
 * Force-push but refuse to clobber unseen remote commits (`--force-with-lease`).
 */
force_with_lease: boolean; 
/**
 * Submodule guard (`--recurse-submodules=check|on-demand`); `None` = no
 * flag (git default / user config).
 */
recurse_submodules?: PushRecurseMode | null }
/**
 * `git push --recurse-submodules` mode - the pre-push guard against
 * publishing a superproject that references unpushed submodule commits.
 */
export type PushRecurseMode = 
/**
 * Abort the push when referenced submodule commits are on no remote.
 */
"check" | 
/**
 * Push the needed submodule branches first, then the superproject.
 */
"on_demand"
/**
 * What a `RebaseStep` does with its commit (the todo keyword).
 * Plan-validity rules (first kept step must be a pick or reword, not
 * everything dropped) are enforced by `validate_rebase_plan` in `cli_impl`,
 * mirrored for UX by `planError` in `planModel.ts` - keep both in sync.
 */
export type RebaseAction = "pick" | 
/**
 * Pick + replace the message (keeping the author). Expands to
 * `pick <sha>` + `fixup -C <carrier>` in the todo; there is no
 * single-line `reword` (it would open an editor, which the runner
 * hardens off).
 */
"reword" | "squash" | "fixup" | "drop"
/**
 * Outcome of `rebase` / `rebase_continue` / `rebase_skip`.
 */
export type RebaseOutcome = { kind: "completed" } | { kind: "already_up_to_date" } | { kind: "conflicts"; message: string } | 
/**
 * The rebase itself finished, but reapplying the `--autostash` produced
 * conflicts; git keeps the stash entry (mirrors `StashPopConflicts`).
 */
{ kind: "completed_with_stash_conflicts"; message: string }
/**
 * Probe for the interactive-rebase panel: which range commits are already
 * on the upstream (warn before rewriting), and whether the base is NOT an
 * ancestor of HEAD (the rebase then RELOCATES the range onto the base).
 */
export type RebaseRangeInfo = { 
/**
 * Range commits NOT reachable from `@{upstream}`; None = no upstream.
 */
unpushed: string[] | null; 
/**
 * True when the base is not an ancestor of HEAD (transplant).
 */
transplant: boolean }
/**
 * One step of an interactive-rebase plan. The slice order is the new commit
 * order, oldest first — exactly git's todo-file order. `Squash` melds the
 * commit into the previous kept step combining both messages (accepted
 * unchanged via `GIT_EDITOR=true`); `Fixup` melds keeping only the previous
 * message. `Reword` picks the commit and replaces its message (author
 * preserved) via a carrier commit + `fixup -C` (git >= 2.32).
 */
export type RebaseStep = { action: RebaseAction; sha: CommitId; 
/**
 * New commit message; required (non-blank) for `Reword`, refused
 * otherwise. `#[serde(default)]` keeps old callers/tests compiling.
 */
message?: string | null }
/**
 * Decoration attached to a commit in `git log --decorate=full` output.
 */
export type RefDecoration = 
/**
 * Bare HEAD (detached).
 */
{ type: "head" } | 
/**
 * HEAD pointing at a branch, e.g. `HEAD -> refs/heads/main`.
 */
{ type: "headOf"; value: string } | 
/**
 * A local branch ref, e.g. `refs/heads/main`.
 */
{ type: "branch"; value: string } | 
/**
 * A tag ref, e.g. `refs/tags/v1.0`.
 */
{ type: "tag"; value: string } | 
/**
 * A remote-tracking branch ref, e.g. `refs/remotes/origin/main`.
 */
{ type: "remote"; value: string } | 
/**
 * A stash entry, carrying its reflog selector (e.g. `stash@{0}`). Synthesized
 * for the commit-graph display; not produced by `git log --decorate`.
 */
{ type: "stash"; value: string } | 
/**
 * Any other ref (notes, stash, …).
 */
{ type: "other"; value: string }
/**
 * One `git reflog` entry (HEAD's reflog). Backs the Reflog view: the safety
 * net for undo — every HEAD movement is here, including resets and rebases.
 */
export type ReflogEntry = { 
/**
 * Positional selector, e.g. `HEAD@{0}`. Display-only: it shifts with
 * every HEAD movement; actions address the entry by `sha`.
 */
selector: string; 
/**
 * The commit HEAD pointed at after this movement.
 */
sha: CommitId; 
/**
 * The action prefix of the reflog subject, e.g. `commit`, `reset`,
 * `checkout`, `rebase (finish)`.
 */
action: string; 
/**
 * The rest of the reflog subject after `action: `.
 */
subject: string; 
/**
 * Committer timestamp of the reflog entry (Unix seconds).
 */
timestamp: number }
/**
 * UI region placement mode. See DESIGN-v0.2.md §C.2.
 */
export type RegionPlacement = "top" | "left"
/**
 * A configured git remote. `git remote -v` always lists a fetch and a push URL
 * per remote; they're equal unless a separate push URL was set.
 */
export type Remote = { name: string; fetch_url: string; push_url: string }
/**
 * Outcome of `checkout_remote_branch`: the switch result plus what the
 * fast-forward step did, so partial success crosses IPC as data.
 */
export type RemoteCheckoutOutcome = { 
/**
 * Short local branch name derived from the remote ref (`origin/x` -> `x`).
 */
local_branch: string; switch: SwitchOutcome; fast_forward: FastForwardResult; 
/**
 * LFS pointer stubs the checkout left behind (see `LfsStubs`).
 */
lfs_stubs: LfsStubs | null }
/**
 * A tag as it exists on a remote (`git ls-remote --tags`).
 */
export type RemoteTag = { 
/**
 * Short tag name (no `refs/tags/` prefix).
 */
name: string; 
/**
 * The commit the remote tag points at (peeled when available).
 */
target_sha: CommitId }
/**
 * Result of `git add --renormalize`: the files git restaged through the
 * clean filter (captured via the dry run immediately beforehand). Empty
 * means the repo was already normalized: an outcome, not an error.
 */
export type RenormalizeOutcome = { restaged: string[] }
/**
 * Preview for the Normalize block: which index entries a renormalize would
 * change, plus how many tracked files carry unstaged changes (restaging
 * stages those edits too - `--renormalize` implies `-u`).
 */
export type RenormalizePreview = { files: string[]; unstaged_changes: number }
/**
 * A single file in the repo-wide Files tree (`list_repo_files`,
 * `list_files_at_revision`).
 */
export type RepoFileEntry = { path: string; kind: RepoFileKind; 
/**
 * The entry is not a blob: a tracked gitlink (mode 160000 / ls-tree type
 * `commit`) or an untracked nested git repo (`ls-files --others` lists it
 * as `dir/`). No file content exists at this path in the object store,
 * so blob actions (View, Blame) don't apply.
 */
submodule: boolean }
/**
 * How git regards a file in the repo-wide Files tree. The three classes are
 * disjoint by git's own definition: a path is either in the index (tracked),
 * or an "other" file that is either ignored or not.
 */
export type RepoFileKind = 
/**
 * In the index (`git ls-files`).
 */
"tracked" | 
/**
 * Present in the working tree, not tracked, not ignored.
 */
"untracked" | 
/**
 * Matched by a gitignore rule.
 */
"ignored"
/**
 * Which multi-step git operation the repository is currently in, if any.
 * Cherry-pick/revert are detected (the banner machinery is shared) even
 * though their UI triggers ship later.
 */
export type RepoOpState = { kind: "none" } | { kind: "merge"; branch: string | null; message: string | null } | { kind: "rebase"; onto: string | null; head_name: string | null; current_step: number | null; total_steps: number | null } | { kind: "cherry_pick"; sha: string } | { kind: "revert"; sha: string }
/**
 * Settings that persist for a specific repo. On disk as
 * `<app-data>/repos/<hash>/settings.json`. Loaded into the `RepoSession`
 * when the repo is opened; persisted eagerly on each change.
 */
export type RepoSettings = { 
/**
 * Per-repo override for the git binary path (None = inherit global).
 */
git_path_override: string | null; 
/**
 * Per-repo override for the Working Changes line-ending chips
 * (None = inherit global).
 */
line_ending_chips_in_changes: boolean | null; 
/**
 * Per-repo override for the commit line-ending warning
 * (None = inherit global).
 */
warn_on_line_ending_commit: boolean | null; 
/**
 * Per-repo override for the external editor command template
 * (None = inherit global; same `$ROOT` semantics).
 */
external_editor_command?: string | null; 
/**
 * Lane locks: pin specific refs to fixed lane indices (versioned envelope).
 */
laneLocks?: LaneLocksDoc; 
/**
 * Selected git profile id (None = no profile selected / inherit global).
 * A *hint* about intent, not the source of truth — the active profile is
 * recomputed from live local config (see `commands/profiles.rs`).
 */
git_profile_id?: string | null; 
/**
 * Auto-update submodule pointers after switch/pull (None = default ON).
 */
submodule_auto_update?: boolean | null; 
/**
 * Show remote-tracking branches in the commit tree (None = default ON).
 */
show_remote_branches?: boolean | null; 
/**
 * Per-repo override for auto-push tags (None = inherit global).
 */
auto_push_tags?: boolean | null; 
/**
 * Suppress the "uses LFS but git-lfs unavailable" warning banner for
 * this repo (None = warn, the default; set via the banner's
 * "Don't warn for this repo" or the Repo Settings panel).
 */
suppress_lfs_warning?: boolean | null; 
/**
 * Commit button default: plain commit vs commit-and-push
 * (None = plain commit). Set only via the button's caret menu.
 */
commit_button_mode?: CommitButtonMode | null }
export type RepoSummary = { id: string; 
/**
 * The repo root as the repo's HOST sees it (display + copy affordances).
 */
path: string; name: string; 
/**
 * `None` = local repo. See `remote::HostRef`.
 */
host?: HostRef | null; 
/**
 * The persistable locator string (bare path for local repos,
 * `wsl://<distro>/<path>` for remote) — what recents/currently-open
 * bookkeeping stores and `open_repo` accepts.
 */
locator?: string; 
/**
 * Why the repo's filesystem watcher failed to start (`None` = watching,
 * or watching deliberately disabled). Feeds the "live updates off" badge
 * on the repo tab. `RepoSession::summary` leaves it `None`;
 * `AppState::attach_watch_error` fills it where state is at hand.
 */
watch_error?: string | null }
/**
 * Mode for `git reset <target>` — what happens to the index and worktree.
 * `Soft` moves HEAD only (changes stay staged), `Mixed` also resets the
 * index (changes become unstaged), `Hard` additionally resets the worktree
 * (destructive — uncommitted changes are lost).
 */
export type ResetMode = "soft" | "mixed" | "hard"
/**
 * The identity git would use for a commit in this repo, resolved across all
 * scopes (local > global > system). Both `None` means a commit would fail
 * with git's "Please tell me who you are" error: the UI surfaces that
 * BEFORE the failed commit (commit-box nudge).
 */
export type ResolvedIdentity = { user_name: string | null; user_email: string | null }
export type RestoreResult = { repos: RepoSummary[]; active_id: string | null }
/**
 * One config key across the scopes; `resolved` is the value git actually
 * uses (local > global > system).
 */
export type ScopedConfig = { local: ConfigValue; global: ConfigValue; system: ConfigValue; resolved: ConfigValue }
/**
 * Outcome of `revert` / `cherry_pick` (and their `--continue` / `--skip`).
 * Both drive git's sequencer, so they share one outcome shape. Conflicts are
 * an outcome, not an error: the sequencer is paused and the user resolves,
 * then continues/skips/aborts.
 */
export type SequenceOutcome = { kind: "completed" } | { kind: "conflicts"; message: string }
/**
 * Author or committer identity recorded on a commit.
 */
export type Signature = { name: string; email: string; 
/**
 * Unix timestamp (seconds since epoch).
 */
timestamp: number; 
/**
 * Timezone offset in minutes (e.g., +120 for UTC+2).
 */
tz_offset_minutes: number }
/**
 * Verification result for a commit signature.
 */
export type SignatureStatus = "Good" | "BadSignature" | "UnknownKey" | "Untrusted" | "Expired" | "Revoked" | "NoSignature"
/**
 * Signing metadata attached to a commit.
 */
export type SignatureVerification = { status: SignatureStatus; signer: string | null; key_id: KeyId | null; raw: string | null }
/**
 * All signing-relevant config keys, each resolved across scopes.
 */
export type SigningView = { 
/**
 * `commit.gpgsign` — whether commits are signed by default.
 */
gpgsign: ScopedConfig; 
/**
 * `gpg.format` — `openpgp` (default), `ssh`, or `x509`.
 */
format: ScopedConfig; 
/**
 * `user.signingkey` — key id (GPG) or key path / literal key (SSH).
 */
signing_key: ScopedConfig; 
/**
 * `gpg.ssh.allowedSignersFile` — required for SSH signatures to verify
 * as trusted rather than merely valid.
 */
allowed_signers: ScopedConfig }
/**
 * One key pair on disk (private key + `<path>.pub`).
 */
export type SshKeyStatus = { 
/**
 * Absolute private-key path.
 */
private_key_path: string; exists: boolean; 
/**
 * Content of `<path>.pub`, when readable.
 */
public_key: string | null }
/**
 * Result of an `ssh -T git@<host>` authentication probe.
 */
export type SshTestOutcome = { kind: "authenticated"; detail: string } | { kind: "rejected"; detail: string } | { kind: "cannot_connect"; detail: string } | { kind: "unknown"; detail: string }
/**
 * Outcome of `git stash apply` / `git stash pop`. A merge conflict is reported
 * as `Conflicts` (not `Err`) because the apply itself partially succeeded and
 * the user must resolve the working tree.
 */
export type StashApplyOutcome = { kind: "clean" } | { kind: "conflicts"; message: string }
/**
 * A single entry from `git stash list`.
 */
export type StashEntry = { 
/**
 * The `N` in `stash@{N}` (0 is the most recent).
 */
index: number; 
/**
 * The reflog selector, e.g. `stash@{0}`. Display-only: it is positional
 * and shifts as stashes are added/removed, so apply/pop/drop/rename
 * address the stash by `stash_sha` instead (resolved back to the current
 * selector at action time).
 */
selector: string; 
/**
 * The reflog subject, e.g. `On main: my message` or `WIP on main: 1a2b3c …`.
 */
message: string; 
/**
 * The stash's own commit SHA (a real git object — usable as a commit id,
 * e.g. for showing its diff). The stable handle for stash mutations.
 */
stash_sha: CommitId; 
/**
 * The base commit the stash was created from (its first parent).
 */
base_sha: CommitId; author: Signature; 
/**
 * Author timestamp (Unix seconds).
 */
timestamp: number }
/**
 * Outcome of `git stash push`.
 */
export type StashOutcome = { kind: "created" } | 
/**
 * The working tree was clean — nothing to stash.
 */
{ kind: "nothing_to_stash" }
/**
 * One submodule's auto-update outcome.
 */
export type SubmoduleAutoUpdateResult = { path: string; status: SubmoduleAutoUpdateStatus; 
/**
 * LFS pointer stubs the move left inside the submodule (see `LfsStubs`).
 */
lfs_stubs: LfsStubs | null }
/**
 * Per-submodule outcome of the post-switch/pull auto-update. Data, not an
 * error: partial success crosses IPC as outcomes (house rule).
 */
export type SubmoduleAutoUpdateStatus = 
/**
 * Clean submodule checked out at the recorded SHA.
 */
{ kind: "updated" } | 
/**
 * Dirty submodule updated with the local changes carried over
 * (TryDirectly carry, or a clean auto-stash pop).
 */
{ kind: "changes_carried" } | 
/**
 * Updated; the changes were deliberately left parked in the
 * submodule's stash (StashAndKeep).
 */
{ kind: "changes_stashed" } | 
/**
 * The auto-stash pop conflicted: the submodule was rolled back to its
 * previous commit and the changes reapplied cleanly there. Nothing was
 * lost; the pointer remains un-updated.
 */
{ kind: "rolled_back"; message: string } | 
/**
 * Worst case: rollback's own pop failed too. The changes are SAFE in
 * the submodule's stash; the submodule sits at its previous commit.
 */
{ kind: "changes_in_stash"; message: string } | 
/**
 * The update could not run (conflicted submodule, checkout refused,
 * fetch failure, ...). The submodule was left untouched.
 */
{ kind: "skipped"; message: string }
export type SubmoduleChange = { path: string; old_sha: CommitId | null; new_sha: CommitId | null; 
/**
 * The submodule worktree has uncommitted content on top of `new_sha`
 * (git's `-dirty` suffix on the `Subproject commit` line).
 */
dirty: boolean }
/**
 * State of a removed submodule's retained gitdir (`.git/modules/<name>`).
 */
export type SubmoduleGitdirInfo = { path: string; 
/**
 * Commits on local branches that are on no remote - deleting the gitdir
 * would destroy them permanently.
 */
unpushed: boolean }
/**
 * Submodule entry as recorded in the superproject. Keyed by `name` (durable
 * across `git mv`: config sections and `.git/modules/<name>` use it);
 * displayed by `path`.
 */
export type SubmoduleInfo = { name: string; path: string; 
/**
 * Effective URL (`.git/config`); `None` when uninitialized.
 */
url: string | null; 
/**
 * URL declared in `.gitmodules` (for drift detection).
 */
gitmodules_url: string | null; 
/**
 * `.gitmodules` `branch` field (used by `update --remote`, tier 3).
 */
branch: string | null; 
/**
 * The gitlink SHA in the superproject index; `None` for a declared-but-
 * never-added entry.
 */
recorded_sha: CommitId | null; 
/**
 * HEAD of the checked-out submodule; `None` when unpopulated.
 */
checked_out_sha: CommitId | null; 
/**
 * The submodule's checked-out branch; `None` = detached HEAD (or
 * unpopulated).
 */
head_branch: string | null; state: SubmoduleState }
/**
 * Commits between two submodule pointers, or the reason they can't be shown.
 */
export type SubmoduleLog = { kind: "commits"; commits: SubmoduleLogEntry[] } | 
/**
 * The target SHA is not present in the submodule's object store - the
 * pointer references an unfetched commit.
 */
{ kind: "target_missing" }
/**
 * One commit in a submodule pointer range (`repo_submodule_log`).
 */
export type SubmoduleLogEntry = { id: CommitId; subject: string }
/**
 * Orthogonal state flags of one submodule. A struct, not an enum: states
 * combine freely (detached AND dirty AND pointer-moved). The UI derives a
 * single display badge by precedence (spec 2026-07-08, "Data model").
 */
export type SubmoduleState = { 
/**
 * Registered in `.git/config` (`submodule.<name>.url` present).
 */
initialized: boolean; 
/**
 * The worktree is checked out (a git repo exists at the path).
 */
populated: boolean; 
/**
 * Checked-out HEAD differs from the SHA recorded in the superproject.
 */
pointer_moved: boolean; 
/**
 * Modified tracked files inside the submodule worktree.
 */
dirty_tracked: boolean; 
/**
 * Untracked files inside the submodule worktree.
 */
dirty_untracked: boolean; 
/**
 * The gitlink is unmerged in the superproject.
 */
conflicted: boolean; 
/**
 * A gitlink with no `.gitmodules` entry.
 */
orphan_gitlink: boolean; 
/**
 * `.gitmodules` URL and effective (`.git/config`) URL disagree.
 */
config_drift: boolean }
/**
 * Options for `submodule update`. Empty `paths` = all submodules.
 */
export type SubmoduleUpdateOptions = { 
/**
 * Also register new submodules (`--init`).
 */
init: boolean; 
/**
 * Recurse into nested submodules (`--recursive`).
 */
recursive: boolean; paths: string[]; 
/**
 * After the update, attach detached submodule HEADs to a branch pointing
 * at the same commit (top-level submodules only). Filled backend-side
 * from the global setting; the frontend never sets it.
 */
attach_branch?: boolean }
/**
 * Integration mode for `submodule update --remote`.
 */
export type SubmoduleUpdateStrategy = 
/**
 * Detach at the fetched commit (git's default).
 */
"checkout" | 
/**
 * Rebase the current branch onto the fetched commit.
 */
"rebase" | 
/**
 * Merge the fetched commit into the current branch.
 */
"merge"
export type SwitchDirtyBehavior = 
/**
 * Let git decide: a non-conflicting dirty tree carries over, a
 * conflicting one fails (`WouldOverwriteLocalChanges`).
 */
"try_directly" | 
/**
 * Stash, switch, pop: the changes travel to the target branch.
 */
"auto_stash" | 
/**
 * Stash, switch, and leave the stash entry parked: the target branch
 * starts clean and the WIP stays retrievable from the stash list.
 */
"stash_and_keep"
export type SwitchOutcome = { kind: "clean" } | 
/**
 * The switch succeeded and (per `StashAndKeep`) the uncommitted changes
 * were deliberately left parked in the stash.
 */
{ kind: "changes_stashed" } | 
/**
 * The switch succeeded and the auto-stash was reapplied, but with merge
 * conflicts: the changes are in the working tree with conflict markers and
 * the stash entry was kept (git's pop-on-conflict behavior).
 */
{ kind: "stash_pop_conflicts"; message: string } | 
/**
 * The switch succeeded but the auto-stash could not be applied at all —
 * the changes remain parked in the stash entry.
 */
{ kind: "stash_pop_failed"; message: string }
/**
 * A switch/checkout's outcome plus any LFS pointer stubs the checkout left
 * behind (see `LfsStubs`).
 */
export type SwitchResult = { outcome: SwitchOutcome; lfs_stubs: LfsStubs | null }
/**
 * A local tag from `git for-each-ref refs/tags`.
 */
export type TagInfo = { 
/**
 * Short tag name (no `refs/tags/` prefix).
 */
name: string; 
/**
 * The commit the tag points at (peeled for annotated tags).
 */
target_sha: CommitId; 
/**
 * Annotated (tag object) vs lightweight (plain ref to a commit).
 */
annotated: boolean; 
/**
 * Annotation subject line (annotated tags only).
 */
message: string | null; 
/**
 * The tagged commit is reachable from a remote-tracking ref. Pushing a
 * tag whose target is NOT on the remote would upload commits no remote
 * branch references, so the UI disables it (push the branch first).
 */
target_on_remote: boolean; 
/**
 * `%(creatordate:unix)` (Unix seconds): the tag object's creation date
 * for annotated tags, the tagged commit's committer date for lightweight
 * ones. Drives the user-selectable ref sort order; 0 when absent.
 */
created_at: number }
export type TextDiff = { old_path: string | null; new_path: string | null; hunks: DiffHunk[] }
export type ThemeEntry = { name: string; source: ThemeSource; path: string }
export type ThemeSource = "builtin" | "user"
/**
 * Ahead/behind tracking status for the current branch relative to its upstream.
 */
export type TrackingStatus = { 
/**
 * The current local branch (short name).
 */
branch: string; 
/**
 * The upstream ref it tracks (e.g. `origin/main`).
 */
upstream: string; 
/**
 * Commits on the local branch not yet on the upstream.
 */
ahead: number; 
/**
 * Commits on the upstream not yet on the local branch.
 */
behind: number }
/**
 * How `worktree add` populates the new worktree.
 */
export type WorktreeAddMode = 
/**
 * Check out an existing local branch (git refuses one that is already
 * checked out in another worktree).
 */
{ kind: "checkout"; branch: string } | 
/**
 * Create a new branch at `start_point` (HEAD when None) and check it out.
 */
{ kind: "new_branch"; name: string; start_point: string | null } | 
/**
 * Detached checkout at `rev` (HEAD when None).
 */
{ kind: "detach"; rev: string | null }
/**
 * One entry of `git worktree list --porcelain`. `path` is the worktree's
 * absolute path ON ITS HOST and doubles as the stable worktree identity
 * (the future mode-B WorktreeId - see
 * design/2026-09-10-worktrees-parallel-graph.md). `head` is carried so the
 * commit graph can decorate worktree HEADs later without a new query.
 */
export type WorktreeInfo = { path: string; 
/**
 * HEAD commit sha; None only for a bare main entry.
 */
head: string | null; 
/**
 * Short branch name (refs/heads/ stripped); None when detached or bare.
 */
branch: string | null; 
/**
 * The first listed entry is the main worktree.
 */
is_main: boolean; detached: boolean; bare: boolean; 
/**
 * Present when locked; the lock reason ("" when none was given).
 */
locked: string | null; 
/**
 * Present when prunable; git's reason.
 */
prunable: string | null; 
/**
 * Whether the checkout has uncommitted changes (untracked included).
 * None when not probed (bare/prunable entries) or the probe failed -
 * the indicator is best-effort, never a blocker.
 */
dirty: boolean | null }
/**
 * One WSL distribution as shown in the picker.
 */
export type WslDistro = { name: string; running: boolean; is_default: boolean }

/** tauri-specta globals **/

import {
	invoke as TAURI_INVOKE,
	Channel as TAURI_CHANNEL,
} from "@tauri-apps/api/core";
import * as TAURI_API_EVENT from "@tauri-apps/api/event";
import { type WebviewWindow as __WebviewWindow__ } from "@tauri-apps/api/webviewWindow";

type __EventObj__<T> = {
	listen: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.listen<T>>;
	once: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.once<T>>;
	emit: null extends T
		? (payload?: T) => ReturnType<typeof TAURI_API_EVENT.emit>
		: (payload: T) => ReturnType<typeof TAURI_API_EVENT.emit>;
};

export type Result<T, E> =
	| { status: "ok"; data: T }
	| { status: "error"; error: E };

function __makeEvents__<T extends Record<string, any>>(
	mappings: Record<keyof T, string>,
) {
	return new Proxy(
		{} as unknown as {
			[K in keyof T]: __EventObj__<T[K]> & {
				(handle: __WebviewWindow__): __EventObj__<T[K]>;
			};
		},
		{
			get: (_, event) => {
				const name = mappings[event as keyof T];

				return new Proxy((() => {}) as any, {
					apply: (_, __, [window]: [__WebviewWindow__]) => ({
						listen: (arg: any) => window.listen(name, arg),
						once: (arg: any) => window.once(name, arg),
						emit: (arg: any) => window.emit(name, arg),
					}),
					get: (_, command: keyof __EventObj__<any>) => {
						switch (command) {
							case "listen":
								return (arg: any) => TAURI_API_EVENT.listen(name, arg);
							case "once":
								return (arg: any) => TAURI_API_EVENT.once(name, arg);
							case "emit":
								return (arg: any) => TAURI_API_EVENT.emit(name, arg);
						}
					},
				});
			},
		},
	);
}
